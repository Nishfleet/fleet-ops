## What changed and why

Retires the three hand-built dead-man canaries per Nish's 2026-09-07 decision on #4146 (umbrella #4140: "nothing hand built"). Net machinery trends negative: ~8,000 lines deleted, 0 added organs.

### (A) gh-webhook-canary-deadman — DELETED (redundant)
Pure dead-man, redundant with the already-live `FleetGhWebhookCanaryAbsent` absent() rule in `config/fleet_rules.yml`. Deleted `bin/gh-webhook-canary-deadman.py`, `systemd/gh-webhook-canary-deadman.{service,timer}`, the deadman prom writer, and deadman blocks in `tests/gh-webhook-canary.test.sh`. Added an optional `GH_WEBHOOK_HEALTHCHECKS_URL` ping-on-success seam to `bin/gh-webhook-canary.py` + `EnvironmentFile=-%h/.config/fleet-ops/gh-webhook-hc.env` to the service (external dead-man; no API key on this box per vacation-audit-20260827.md, so the absent() rule is the primary replacement — the healthchecks.io seam is ready for when Nish creates the check via the web UI).

### (B) fleet-completion-canary — DELETED (producer retired, no replacement)
Produced the `fleet_chain_*` metric family and walked the alert-repair chain. Deleted `bin/fleet-completion-canary.py` (2,039 lines), `systemd/fleet-completion-canary.{service,timer}`, `tests/fleet-completion-canary.test.sh` (2,539 lines), `fleet-chains.prom`. Removed `FleetCompletionCanaryAbsent`, `FleetChainStalled`, `FleetSloChainRepairLatencyOverTarget` from `config/fleet_rules.yml`, the `chain_repair_latency` SLO from `config/slo-definitions.json`, and the SLO instrumentation from `libexec/fleet-metrics-export.py` + `lib/slo_budget.py`. No replacement producer — chain latency is re-derived from LiteLLM/Postgres logs after #4130 if needed.

### (C) fleet-loose-ends-canary — DELETED (off-the-shelf replacements)
Ticket-filer for half-done PRs, unanswered QUESTIONS.md rows, and stale worktrees. Deleted `bin/fleet-loose-ends-canary`, `lib/loose-ends.py`, `systemd/fleet-loose-ends-canary.{service,timer}`, `tests/fleet-loose-ends-canary.test.sh`. Replacements:
- **C1** open PRs >24h without auto-merge → GitHub `actions/stale` per repo (follow-up issues filed: 0509#1901, siterep-public#90, inish-site#143)
- **C2** unanswered QUESTIONS.md rows → guardrail #6 in the hourly judge packet (`agent-state/fleet-landing-watch/fable-check.md`): re-ask any row >24h in its Telegram line
- **C3** stale worktrees → already covered by the worktree reaper under #4149

### install.sh
`remove_retired_canaries()` stops+disables+removes the live units and strips the retired prom files on next install.

### Vault ledger
One line appended to `_system/shared-memory/retired-mechanisms.md` (acceptance #3).

## Verification

Exact commands and observed output:

```
$ bash tests/gh-webhook-canary.test.sh
OK: 1: script compiles
OK: 2: DRY output: well-formed HMAC + issues event + agent-ready label + synthetic repo
OK: 3: HMAC header matches secret for the synthetic body
OK: 4: healthchecks.io ping-on-success seam prints in DRY mode
OK: 4b: empty healthchecks.io URL stays silent

$ bash tests/manifest-shape.test.sh && bash tests/system-dropins-shape.test.sh && bash tests/slo-budget.test.sh
all PASS

$ bash tests/timer-manifest.test.sh
OK: all 81 live user timers have manifest entries (86 manifest entries total)
SKIP: live timer 'fleet-completion-canary.timer' missing from LOCAL manifest but present on origin/main — checkout is behind, not a real gap
SKIP: live timer 'fleet-loose-ends-canary.timer' missing from LOCAL manifest but present on origin/main — checkout is behind, not a real gap
SKIP: live timer 'gh-webhook-canary-deadman.timer' missing from LOCAL manifest but present on origin/main — checkout is behind, not a real gap

$ bash tests/canary-effectiveness.test.sh && bash tests/escalation-coverage-canary.test.sh && bash tests/fleet-review-arm-check.test.sh
all PASS

$ bash tests/fleet-metrics-export.test.sh && bash tests/fleet-rules-severity-page.test.sh && bash tests/alertmanager-routing-matrix.test.sh
all PASS

$ bash tests/fleet-escalation-completion.test.sh && bash tests/prometheus-retention-40d.test.sh && bash tests/phone-chokepoint.test.sh
all PASS

$ for t in tests/alert-repair-*.test.sh; do bash "$t"; done
all PASS (class-park-skip, fleet-escalation-storm-skip, outcome-metric, seat-walled, slo-slowburn-skip, wfr-trend-skip, claim-mutex)

$ sgscan --base origin/main
No new security findings.   exit 0
```

Replacement live proof (acceptance #1):
- `FleetGhWebhookCanaryAbsent` absent() rule is live in Prometheus (verified: `curl -s 127.0.0.1:9090/api/v1/rules` shows it loaded) — the primary replacement for (A).
- The healthchecks.io ping-on-success seam is proven via `tests/gh-webhook-canary.test.sh` OK 4 (DRY mode prints the ping) and OK 4b (empty URL stays silent).
- (B) has no replacement producer by decision.
- (C2) judge packet rule is live in `fable-check.md` guardrail #6 (next hourly judge run will enforce it).

run-proof: `tests/gh-webhook-canary.test.sh` (healthchecks seam green), `tests/timer-manifest.test.sh` (retired timers correctly absent from manifest, SKIP on live), `tests/slo-budget.test.sh` (chain_repair_latency SLO removed, 6 SLOs remain), `tests/canary-effectiveness.test.sh` (organ registry updated), `tests/fleet-escalation-completion.test.sh` (chain tracking without completion-canary), `sgscan` (clean).

research: in-repo authoritative sources read before editing — `config/fleet_rules.yml` (FleetGhWebhookCanaryAbsent already live at :1021), `bin/gh-webhook-canary.py` (the producer that stays), `config/slo-definitions.json` (chain_repair_latency SLO), `lib/slo_budget.py` (SLO instrumentation), `libexec/fleet-metrics-export.py` (chain_repair_latency exporter), `bin/keystone-hc-ping` (existing healthchecks.io pattern), `agent-state/alert-repair/vacation-audit-20260827.md` (no HC_API_KEY on this box). Per the issue's Prior art: the absent() rule already existed; no new organ, no new bin/ file.

help-first: no new bin/ file shipped; `install.sh remove_retired_canaries()` uses the existing install.sh cleanup pattern (same shape as `remove_orphaned_fleet_cheap_triage_dropin`).

loose-ends-canary: fleet-loose-ends-canary retired (fleet-ops#528); follow-up issues 0509#1901, siterep-public#90, inish-site#143 for actions/stale; judge packet guardrail #6 for QUESTIONS.md re-ask.

Closes #4146
