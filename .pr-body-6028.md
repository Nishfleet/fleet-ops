## Summary

Closes #6028

Spec line `required:` on #6028 — "port both to the litellm-era contract or delete them, and wire the survivor into ci.yml". Live-state at HEAD (`32bed3c17`, #6037, landed 14:59Z — 29min after the issue was filed at 14:30Z) already did the port:

- `tests/pi-issue-run-empty-success.test.sh` — ported to the litellm-era seat contract (asserts `class=empty-success seat=litellm/<model> output_bytes=<n>`) and hosted from `tests/seat-lib.test.sh:88` (`bash "$here/pi-issue-run-empty-success.test.sh"`), which ci.yml lists directly (line 139). Passes (rc=0).
- `tests/pi-issue-run-hang-window-scale.test.sh` — deleted by #6037 (its `mark_seat_hang_bench` expectations were litellm-era rot; proxy cooldown owns routing). The surviving coverage is `tests/pi-issue-run-hang-stall-bench.test.sh`, hosted from `tests/ci-standards-audit.test.sh:651` (file listed in ci.yml at line 219). Passes (rc=0).

Both survivors are reachable in the #5889 closure (listed+hosted: 494) — neither rides the auto-host glob — so the "wire the survivor into ci.yml" clause is satisfied the only way a worker PR can: invocation from a ci.yml-listed test (the worker App cannot push `.github/workflows/**`, fleet-ops#5889/#3235; a direct ci.yml line is mechanism-impossible for this token).

What #6037's rushed repair did not leave behind is the **named pin** for either hosted test. This PR adds the two missing pins to `tests/p14-test-listing-gate.test.sh` (same shape as the #5870/#4263 pins): a future drop of either host line now fails the P14 gate BY NAME instead of parking the test silently on the #5889 auto-host or `known_orphans`.

No host lines change; both tests pass at HEAD; the gate accounting closes at 517 test files.

Relates to #6028

## Verification

- `bash tests/pi-issue-run-empty-success.test.sh` → `OK: fleet-ops#4457: SUCCESS-no-PR is classed empty-success (verdict line), not benched, shipped control stays clean` (rc=0)
- `bash tests/pi-issue-run-hang-stall-bench.test.sh` → `OK: pi-issue-run #2133 hang/stall detectors fire and bench the seat via mark_seat_hang_bench` (rc=0)
- `bash tests/p14-test-listing-gate.test.sh` → `OK: pi-issue-run-empty-success.test.sh host line in seat-lib.test.sh is pinned (fleet-ops#6028)` + `OK: pi-issue-run-hang-stall-bench.test.sh host line in ci-standards-audit.test.sh is pinned (fleet-ops#6028)` + `OK: all 517 test files accounted for (listed+hosted: 494, live skip: 5, known orphan: 15, auto-hosted: 3)` + `P14 test list is closed` (rc=0)
- `shellcheck tests/p14-test-listing-gate.test.sh` → 0 findings
- `bash bin/sgscan --base origin/main` → `No new security findings` (rc=0)

net-positive-because: adds the two by-name pins the #6028 wiring clause needs to be durable — without them a future host-line drop silently demotes a repaired test to the #5889 auto-host with no named failure (the #5620/#5622 rot class); 35 pinned lines guard the port #6037 paid a red-main repair for.

run-proof: not machinery (no unit/timer/workflow) — test-only PR; the P14 listing gate and both pinned survivors ran green above (rc=0), and the same gate chain runs in CI on this PR.

organ-heartbeat: tests/p14-test-listing-gate.test.sh not-an-organ: tests-file, not a config/unit/workflow surface.

loose-ends: none — pins are the whole delta; the survivors were already ported and hosted by #6037, and the P14 CI check on this PR is the observe-to-close.
