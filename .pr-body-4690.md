Why:
From 2026-09-08 every `devin/glm-5-2` run ended `class=empty-success`. `gh` inside `devin --sandbox` talks to a proxy at localhost:3128. `/etc/hosts` had lost `127.0.0.1 localhost`, so the sandbox could not resolve the proxy, the seat looked healthy, and 15-min comeback windows re-released it 10+ times.

This is a host class, not a seat 900s empty-run. Hold every Devin seat while IPv4 localhost is missing, fail loud in the SuperGrok live-validate tick, and classify the live signature `error connecting to localhost` as `sandbox-localhost-unresolvable`.

Scope:
- `bin/fleet-seat-live-validate`: `getent ahosts localhost | grep -q '^127.0.0.1'` (test seam `FLEET_SANDBOX_LOCALHOST_AHOSTS`). On failure, LOUD line names `/etc/hosts` and reason `sandbox-localhost-unresolvable`, marks every `providers.devin.models` seat, exits 1.
- `lib/seat-lib.sh`: `seat_usable` refuses `devin/*` while that class is true, before spawn-bench/ledger, so a healthy probe cannot re-offer the seat. `is_sandbox_localhost_error` / `classify_death_error` name the class.
- `bin/pi-issue-run`: that signature is not empty-success and not a 900s empty-run bench. A shipped PR URL still wins.
- Live `~/.pi/agent/extensions/seat-health.ts` `classifyCliOutput`: same signature on exit 0 is `transient_fault` / `sandbox_localhost` (backoff 0), not healthy and not empty_run 900s.

09-08 writer (required): no fleet script writes `/etc/hosts` (repo, `/usr/local/sbin`, `nish-maintenance-run`). `restic-r2-restore-test` restores `/etc/hostname` into a temp dir under `ProtectSystem=strict`. cloud-init lists `update_etc_hosts` but `manage_etc_hosts` is not True and last reboot was 2026-08-30. First live sighting: `pi[1292392]` at 2026-09-07T19:26Z already found localhost missing, before the 10:51Z window. No recurring fleet writer to disable. The detector is the stop: a future hosts regression benches Devin within one validate tick.

must-not: no new timer or unit; Devin caps unchanged; sandbox flag untouched.

Verification:
  $ getent ahosts localhost | grep -q '^127.0.0.1'; echo $?
  0
  $ bash -n bin/fleet-seat-live-validate bin/pi-issue-run lib/seat-lib.sh
  $ bash tests/fleet-seat-live-validate.test.sh
  OK: scenario15: missing 127.0.0.1 localhost fails loud, names /etc/hosts, marks all devin seats sandbox-localhost-unresolvable
  $ bash tests/pi-issue-run-empty-success.test.sh
  OK: sandbox-localhost signature is class=sandbox-localhost-unresolvable, not empty-success, not 900s empty-run
  $ bash tests/seat-health-classifier.test.sh
  OK: inv6: exit-0 error connecting to localhost is transient_fault (fleet-ops#4690)

run-proof: live-validate scenario15 + empty-success (d) + seat-health inv6 + seat_usable getent hold, 2026-09-09T15:50Z. Heartbeat-tier1 already invokes fleet-seat-live-validate (scenario11 unchanged). sgscan --base origin/main: "No new security findings."

organ-heartbeat: bin/fleet-seat-live-validate not-an-organ: existing SuperGrok live-validate canary, not a new unit.

loose-ends: none. Live seat-health.ts is out of repo (same as fleet-ops#1466); wrapper-side classifier is in this PR.

Closes #4690
