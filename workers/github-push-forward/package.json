{
  "name": "github-push-forward",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that verifies the GitHub webhook HMAC and forwards the original body through a Cloudflare Tunnel to the VPS-side gh-webhook-receiver (fleet-ops#1464). Dumb transport: verify, forward, 200.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test --experimental-strip-types src/index.test.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240821.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.0"
  }
}
