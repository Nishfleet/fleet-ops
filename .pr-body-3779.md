## What

Verification record for the 2026-09-05T23:15Z FleetMainRed firing on
fleet-ops main (`fleet_main_ci_green{Nishfleet/fleet-ops}=0` at snapshot,
all 8 other enrolled repos green). #3779 is a duplicate alert for the same
firing that already-filed #3748 covered; it was filed ~1h later, once the
`for: 30m` window elapsed and the alert escalated from pending to critical.
The repair had already landed before #3779 was filed.

## Root cause (verified live, identical to #3748)

PR #3740 (`feat(waste-cut): … close-and-archive-repo tool …`, commit
`385ac2e8`, merged 2026-09-05T22:50:42Z) added
`tests/fleet-close-and-archive-repo.test.sh` but did NOT list it in
`.github/workflows/ci.yml`'s P14 reachable set. The `p14-test-listing-gate`
failed on the next push and turned main red. The worker token has no
workflow scope, so the author of #3740 could not edit `ci.yml` itself.

## Repair already landed (not shipped here)

Fix #3765 (commit `4063c127`, merged 2026-09-05T23:58:27Z) listed the test
in `ci.yml` with a citation comment. Main CI went green on the next push
and has stayed green since. See
`reports/main-ci-red-incident-3748-2026-09-06.md` for the incident writeup.

## Verification (fresh dispatch, not a rerun)

The alert input is the point-in-time Prometheus metric:
`fleet_main_ci_green{repo="Nishfleet/fleet-ops"}` = `1` (green) at
dispatch, all 9 enrolled repos green. The FleetMainRed rule
(`expr: fleet_main_ci_green == 0`, `for: 30m`) is therefore not firing.

A confirmed-green **push-triggered** CI run on main — **`34050935200`**
(created 2026-09-06T18:11:45Z on commit `7b784862`, an ancestor of the
then-current main HEAD) — all 5 jobs SUCCESS (systemd-analyze, P14
tests/PR checks, Gitleaks, Shellcheck, Semgrep). (Later pushes while this
record was written were auto-superseded — P14 cancelled by the next push
— their non-P14 jobs all SUCCESS; the metric stayed `1` throughout.)

run-proof: `gh run view 34050935200 -R Nishfleet/fleet-ops` (push,
conclusion success, all 5 jobs success); `curl '.../api/v1/query?query=fleet_main_ci_green'
` → `fleet_main_ci_green{repo="Nishfleet/fleet-ops"}=1`; local worktree
tests: `bash tests/fleet-close-and-archive-repo.test.sh` (ALL 5 SCENARIO
CHECKS PASSED, exit 0); `bash tests/repo-standards.test.sh` (5 passed, 0
failed, exit 0).

## Mechanical-fix

Prevention for this failure class already exists and is proven (no new
gate shipped here):
- `p14-test-listing-gate` — rejects any test file not listed in `ci.yml`
  (nor hosted/live/destructive/known-orphan); it surfaced #3740's omission.
- red-on-main green resolver (`bde3dbf8`, #3711) — auto-closes
  `red-on-main` labelled alert issues once the workflow's latest completed
  main run is green.

#3765 closed the gap; the alert ceased because main is green. This PR
records the verification and closes the residual manually-filed alert so it
stops polluting the agent-ready queue.

net-positive-because: adds one verification record report under
`reports/` (the established pattern for red-on-main incidents #3584, #3626,
#3748); it is paper, not machinery, and closes a duplicate alert issue.

Closes #3779
