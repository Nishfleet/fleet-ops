## What

Two commits on the same alarm class:

1. `fix(signal-reconcile)`: route `AUDITOR-PANEL-PENDING` filings to the `observe-to-close` label instead of `agent-ready` — the same change fleet-ops#4966 shipped for `DEGRADED-LANES`, for the same reason. A pending senior panel is load-borne (the per-tick start cap defers seat starts under backlog) and self-heals via stale-SKIP recast (#3962) and SKIP-EXHAUSTED abstention (#4503). There is no manual worker action: identical filings #4812 (candidate 2182) and #4877 (candidate 2284) closed via observe-to-close with zero worker code, and #4965 alone burned **7 claim cycles and 2 StartLimitBursts** on workers that re-verified the alarm and exited with no PR.

   - New filings carry `observe-to-close`, so the intake (`gh issue list -l agent-ready`) never claims them.
   - On the next tick where the alarm still fires, the existing dedupe path retroactively re-labels #4965 `agent-ready`/`agent-in-progress` -> `observe-to-close` — the claim loop stops.
   - When every pending panel convenes (signal absent from the tick), the reconciler closes the issue as usual.

2. `fix(auditor)` (salvaged from a prior worker's unpushed commit on this claim branch): a stale-SKIP recast re-start now bypasses the per-tick start cap. Without this, a recast whose re-start loses the cap race is deferred — the renamed-aside vote stays MISSING, the recast counter can never advance, the SKIP-EXHAUSTED abstention (#4503) can never fire, and the panel wedges at 2 real votes + 1 missing while AUDITOR-PANEL-PENDING fires every tick. Same carve-out rationale as `recover_failed_audit`; the pi-audit.slice TasksMax/CPUQuota ceiling still bounds real concurrency.

## Why now

#4965's alarm for candidate 2581 is already green (devin/free-glm/senior all PASS — verified live in `~/.local/state/fleet-audit/0509/2581/`), but the signal key `loud/auditor-panel-pending/candidate-age_s-active-missing-failed` is shared by every pending candidate, so the issue stays open while the 0509 backlog drains (candidates 2576/2564/2624 still pending on the 17:49Z tick — load-borne, self-healing). Each open tick under `agent-ready` costs another claimed worker run.

Verification:
- `bash tests/signal-reconcile.test.sh` — ALL PASS, including new scenario 16: 16a fresh filing lands `observe-to-close` not `agent-ready`; 16b still-alarmed open filing is deduped + retroactively re-labeled (never closed while alarmed); 16c green tick observe-to-closes it.
- `bash tests/fleet-heartbeat-auditor.test.sh` — ALL PASS, including scenario17: a SKIP recast re-start bypasses the tick cap while the cap still binds normal starts.
- Live check: `ls ~/.local/state/fleet-audit/0509/2581/` shows devin.vote=PASS, free-glm.vote=PASS, senior.vote=PASS; triage `AUDITOR-PANEL-PENDING` last fired for 2581 at 15:50Z and has not since (16:50Z/17:49Z ticks fire only for other candidates).

run-proof: tests/signal-reconcile.test.sh scenario 16a/16b/16c + tests/fleet-heartbeat-auditor.test.sh scenario17 green (run output above); no new units/timers/workflows added.

net-positive-because: +80 lines of test+comment for a one-line routing fix and a +35-line auditor carve-out — the tests are the durable part (mechanical-fix gate); no new running machinery.

research: reuses the existing observe-to-close routing and retroactive re-label machinery (fleet-ops#4966/#4987, `routing_labels()` + dedupe-path `gh_edit_labels`) — no new organ, detector, or mechanism.

help-first: no new bin/ file; change is inside the existing `lib/detector-queue-reconciler.py` `routing_labels()` and `bin/fleet-heartbeat-auditor` SKIP-recast path.

organ-heartbeat: lib/detector-queue-reconciler.py + bin/fleet-heartbeat-auditor — not-an-organ: neither is in config/fleet-organs.json; both are invoked by the existing fleet-heartbeat tick which already carries the heartbeat metric.

Note: this PR deliberately does NOT use "Closes #" — the issue body requires the detector→queue reconciler to observe green and close it (observe-to-close, fleet-ops#362). The reroute happens on the next dedupe tick after merge.

Relates to #4965
