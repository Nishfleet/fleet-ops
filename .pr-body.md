## Summary

`bin/chain-e2e-drill`: a synthetic-fault rehearsal that proves the whole failure chain end to end — the DONE-proof for #375. It injects one unmistakably-synthetic fixture fault and asserts each hop mechanically by invoking the REAL hop mechanisms (no new hop logic):

1. **fixture-isolated** — `chain-e2e-drill-fixture.service` is non-installable, `RuntimeMaxSec`-bounded, isolated to `chain-e2e-drill.slice`, tagged `[drill:chain-e2e]` with `signal: chain-e2e-drill/fixture`, and excluded from real escalation in `fleet-escalation-canary` (so it never pages a senior or auto-reverts).
2. **ticket-auto-filed** — the #362 detector→queue reconciler auto-files a ticket carrying the `chain-e2e-drill/fixture` signal key.
3. **escalate-senior-routed** — serious classification labels the filed ticket `escalate-senior` (→ #234/#223 senior-auditor panel intake; no real senior page because the unit is escalation-excluded).
4. **mechanism-gate** — #366 gate `REJECT`s a fixture fix with no mechanism and `PASS`es the same fix with this drill + its regression test as the mechanism.
5. **observe-to-close** — the ticket refuses to close while the fixture detector stays red, and flips closed only once green is observed on a real tick.
6. **slo-snapshot** — the result lands in the #180 gap-closure SLO snapshot (`chain_e2e_drill_pass_rate`), and a failing hop auto-files a real LOUD finding.

It piggybacks on the **fleet-blind-audit cadence** (no new timer), per the issue default.

## Verification

Dry-run (`CHAIN_E2E_DRY_RUN=1`) of `bin/chain-e2e-drill`, hermetic (fake gh + issue-file wrappers, no network/systemd):

- `drill --help` → exit 0, prints usage.
- `tests/chain-e2e-drill.test.sh` → PASS (all six hops).
- Reconciler run proofs: hop 2 `filed chain-e2e-drill/fixture -> #375`; hop 5 red `alarms=1 filed=0 deduped=1 heartbeat=1 closed=0`, then green `alarms=0 closed=1`.
- Gate `evaluate` no-mechanism → exit 1 / `REJECT`; with-mechanism → exit 0 / `PASS`.
- `fleet-gap-closure-slo` (GH=/bin/true, scratch state) → `{green:true, chain_e2e_drill_pass_rate:1}`.
- Existing suites rerun green: `failure-mechanism-gate`, `signal-reconcile`, `fleet-gap-closure-loop`, `fleet-blind-audit`, `fleet-heartbeat-blind-audit-*`, `p14-test-listing-gate`.

## run-proof

net-positive-because: adds the whole-chain DONE-proof (drill + hermetic test + fixture units + wiring), which is net-new capability, not a rebuild, masking or token cost.

- Unit run: `tests/chain-e2e-drill.test.sh` (`suite = ci-standards-audit`, hosted — the worker App cannot push `.github/workflows/**`; `p14-test-listing-gate` passes).
- No new timer. Cadence = existing `fleet-blind-audit` (once per audit cycle). New units: `fleet-chain-e2e-drill.service`, `chain-e2e-drill-fixture.service`, `chain-e2e-drill.slice` (isolated fault plane).

## research

research: checked official docs (--help of `fleet-gap-closure-drill`, `fleet-failure-mechanism-gate`, `signal-reconcile` #362) plus the issue's own fixture spec; compared folding the drill into `fleet-gap-closure-loop` vs the existing `fleet-blind-audit` cadence — adopted the issue default (piggyback only, no new timer). The drill calls the existing gates, it does not hand-build them.

## help-first

help-first: read `fleet-gap-closure-drill --help` and `fleet-failure-mechanism-gate --help` — the former proves three detector proofs, the latter is a single-issue evaluator; neither does not compose the whole six-hop chain into one DONE-proof, so `chain-e2e-drill` is warranted and its own `--help` ships.

## loose-ends

`loose-ends: hop-6 live-panel-intake` — hop 3 asserts the escalate-senior routing label + panel-mechanism wiring in dry-run; the live senior-panel intake run is exercised by the real fleet on the audit cadence, not in this PR's hermetic test.

## Test plan

`tests/chain-e2e-drill.test.sh` (hosted in `ci-standards-audit.test.sh`), plus the reopened affected suites above.

Closes #375