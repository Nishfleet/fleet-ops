## Part 2: pin mergegateway quoted quota-bench default

Part 1 (the `is_quota_cap_error` literal + seat-lib test 9b) landed in #3981.
This is the part-2 gap the orchestrator left open: mergegateway is a live
audition provider, so it needs a repo-config `quota_bench_default_s` for
`mark_seat_quota_bench` to bench it instead of failing open.

`config/seat-caps.json`: add `providers.mergegateway` with
`quota_bench_default_s: 604800` (7-day prepaid-refill class, same as the
straitly/xai 402s) at the audition lane's live shape (cap 1, class metered,
audition true) and a dated/measured citation comment. Xkiro is the existing
audition-provider precedent for an audition row carrying a quota default.

No cap change, no seat retirement (infra/money class, rc=1 at 1s 0 tools).

## Verification

- `jq '.providers.mergegateway.quota_bench_default_s' config/seat-caps.json` -> `604800`
- `provider_quota_bench_default mergegateway` -> `604800`
- `is_quota_cap_error` on the exact live tail
  `402: {"message":"Credit balance depleted. Add credits to continue.","type":"budget_exceeded","code":"budget_exceeded"}`
  -> matched (quota/cap wall)
- transient `429 Too Many Requests retry-after: 30` -> NOT matched
- `mark_seat_quota_bench mergegateway google/gemini-3.7-flash` with no window
  text -> wrote ledger `health_class=quota_bench`, `bench_until` 604800s out
  (2026-09-13T18:50Z), `consecutive_failure_count=1` — benched once, not daily

## run-proof

- `bash tests/seat-lib.test.sh` -> exit 0
- `bash tests/seat-lib-aimd.test.sh` -> exit 0 (All AIMD invariants passed)
- `bash tests/fleet-token-economy.test.sh` -> exit 0
- `bash tests/seat-caps-citation.test.sh` -> exit 0
- `bash tests/audition-lane.test.sh` -> exit 0 (ALL AUDITION-LANE TESTS PASSED)
- `bash tests/fleet-ops-deploy.test.sh` -> exit 0
- inner-loop end-to-end `mark_seat_quota_bench` write proven above

net-positive-because: pins a durable config default so a real money wall
(mergegateway 402 budget_exceeded) benches a seat once for 7d instead of the
reactive 24h ledger and daily re-dies; the 14 added lines are one cited config
row with its citation, not new machinery.

Closes #3973
