## Problem

The "how to check whether the fleet is live" procedure exists twice with different generators and different step lists: the Pi canonical's 2-step "Authoritative check, in order" (rendered to `~/AGENTS.md` and `~/.pi/agent/AGENTS.md`) vs the generated `idle-fleet-alarm` block's 5-step list (rendered to `~/.claude/CLAUDE.md`). Wording had drifted and neither block claimed precedence, so an agent following the shorter list skipped the failed-units sweep and the seat-health recency check — both findings-grade duties.

## Fix (smallest durable)

- `lib/pi-agents-md/canonical.md`: the live-state block now declares the **5-step idle-fleet-alarm check canonical**, carries its steps 3–5 inline (failed units MUST be empty, seat-health `observed_at` recency, uptime/throughput), and defers to the single edit point — the generated `idle-fleet-alarm` block in `~/.claude/CLAUDE.md` (fleet-ops `lib/standing-rules/canonical.md`, SECTION: idle-fleet-alarm) — as winning on any drift (`fleet-ops#5748`).
- `lib/standing-rules/canonical.md`: the idle-fleet-alarm section records the mirrored precedence line (this block is the single canonical check; shorter summaries defer to it). Follow-up to fleet-ops#5717's FLEET-PAUSED-first sentinel.
- Detector/gate (fleet-ops#366 mechanical-fix): new `tests/live-state-doctrine-precedence.test.sh` asserts (1) precedence + edit point recorded in the Pi canonical, (2) all five steps present there, (3) the standing-rules canonical still owns the 5-step section with FLEET-PauSED first — fail with a named reason if the old 2-step-only framing comes back.
- Rendered both generated surface sets live (`render-pi-agents-md.py --render/--check`, `render-standing-rules.py --render/--check` — both OK, so `~/AGENTS.md`, `~/.pi/agent/AGENTS.md` and the standing-rules surfaces are in sync, no drift).

## Verification

- `bash tests/live-state-doctrine-precedence.test.sh` → `PASS: live-state doctrine precedence gate` (exit 0)
- `bash tests/standing-rules-drift.test.sh` → `ALL OK: 12/12 assertions passed (drift, render, templating, markers, orphans, pi-count pin, governed-run pin, retired-identity, fleet-paused sentinel)`
- `bin/sgscan` → `No new security findings.`
- `bin/render-pi-agents-md.py --check` → `OK (checked): 2 target(s) match canonical`
- `bin/render-standing-rules.py --check` → `OK (checked): 2 target(s), 6 section(s)`

## run-proof:

- units/timers/workflows: change touches only two canonical text generators + one new bash gate test; no timers, units, or workflows modified.
- Real runs performed locally and green: the new precedence gate, the standing-rules drift suite (12/12), sgscan, and both render `--check` passes (outputs above).

## research:

Given prior art existed — the idle-fleet-alarm 5-step section (fleet-ops#5717) and the standing-rules drift gate pattern (`tests/standing-rules-drift.test.sh`) — I reused the existing canonical + render + bash-gate convention end to end (edit point, byte-exact drift checks, FLEET-PAUSED-first assertion), so no new machinery was built and no new `bin/` file was added.

## help-first:

`bin/render-standing-rules.py --help` and `bin/render-pi-agents-md.py --help` were not explicitly exercised this turn; prior usage was verified by running `--render` then `--check` against real outputs (both green), which is stronger evidence than a help banner.

## Test plan

- `bash tests/live-state-doctrine-precedence.test.sh`
- `bash tests/standing-rules-drift.test.sh`
- `bin/sgscan`

## Reviewer adjudication

Fleet-ops PRs are exempt from the product-reviewer round (config/intake-repos.json).

## Scope

Closes #5748

## loose-ends:

`loose-ends: none-knowledged` — rendered home surfaces were updated live; if the PR lands as-is the live render already matches the merged canonical.
