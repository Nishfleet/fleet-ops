# Self-trigger guard for the escalation layer: reset the global OnFailure=
# (from service.d/10-escalate.conf) to empty so unit-escalation@* instances
# never recurse into themselves when they fail. Defense in depth alongside
# the name guard in unit-escalation-write.
[Unit]
OnFailure=
