# fleet-ops#428: pi-transport-check is a fleet health probe. Its failure is
# NOT a direct Telegram page. Reset the old OnFailure= notify and keep only
# the unit-escalation drop-in path (STOP-REASON -> senior auditor).
[Unit]
OnFailure=
OnFailure=unit-escalation@%n.service
