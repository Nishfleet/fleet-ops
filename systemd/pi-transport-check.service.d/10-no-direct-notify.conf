# fleet-ops#428: pi-transport-check is a fleet health probe. Its failure is
# NOT a direct Telegram page from the probe itself.
# Glue sweep 2026-09-18: the unit-escalation@ / STOP-REASON senior-auditor
# path this used to point at was deleted with the escalation tower. The
# rate-limited pager is the remaining real action, so name it here instead of
# leaving the unit with no OnFailure at all.
[Unit]
OnFailure=
OnFailure=fleet-heartbeat-failed-notify.service
