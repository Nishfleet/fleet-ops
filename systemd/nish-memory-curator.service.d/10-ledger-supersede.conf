# fleet-ops#389: after memoryctl curate + review, flag compiled / agent
# memories whose claims a new decisions-ledger line contradicts.
# Marker only — SUPERSEDED-BY:<date+title> — no delete, no rewritten prose.
# No leading '-': a failed pass must fail the curator unit.
[Service]
ExecStart=/usr/bin/python3 /home/nish/.local/bin/memory-ledger-supersede
