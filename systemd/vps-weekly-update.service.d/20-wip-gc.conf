# fleet-ops#1204: weekly GC for wip/<unit>-<ts> salvage branches.
# Delete only when merged/superseded (three-proof) or older than 14d with
# no dispatch-ledger reference. Leading '-' so a GC fault cannot skip apt.
[Service]
ExecStartPost=-/home/nish/.local/bin/pi-salvage-worktree --gc
