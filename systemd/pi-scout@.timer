[Unit]
Description=Pi fleet product scout schedule for Nishfleet/%i (every 2h)
# supply generation is inherently periodic; intake claiming stays event-gated by its ExecCondition

[Timer]
OnCalendar=*-*-* 00/2:00:00
RandomizedDelaySec=300
Persistent=true

[Install]
WantedBy=timers.target
