# Point intake-reconcile at the live deploy checkout, not the stale
# products/fleet-ops worktree. That checkout can lag origin/main and
# re-enable deferred units (siterep-public, 2026-08-26).
[Service]
Environment=INTAKE_RECONCILE_INTAKE_JSON=/home/nish/workspaces/tooling/fleet-ops-deploy-clone/config/intake-repos.json
