# Every agent runner runs in agent.slice. Per-runner caps; the slice caps
# the group. Runner count is the concurrency bound (README "Worker RAM
# admission"): 6 enabled, matching vars.AGENT_MAX_PARALLEL.
[Service]
Slice=agent.slice
MemoryHigh=3G
MemoryMax=6G
