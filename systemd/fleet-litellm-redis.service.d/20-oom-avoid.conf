# fleet-ops#4861: mark the LiteLLM Redis organ avoid so systemd-oomd
# deprioritizes it under user-slice pressure instead of killing the router
# fast path first.
[Service]
ManagedOOMPreference=avoid
