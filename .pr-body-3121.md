## Audit panel unchoke: senior role replaces the dead straitly seat; owner-authored issues bypass the panel; tally rejects keyword-only reasons

**fleet-ops#3121.** The senior-auditor admission panel was starved because the
`straitly` auditor role resolved only within a provider that is a 402 money
wall (`pi --print --provider straitly --model gpt-5.6-sol` -> `credit balance
exhausted`, Nish-reserved), so every `pi-audit@...--straitly` unit failed
instead of rotating to a live seat. This PR replaces the dead `straitly`
role everywhere with a `senior` role that reads a single ordered ladder
(`senior_seats_in_order` in `config/seat-caps.json`) and resolves to the
first usable seat — a walled seat is skipped, never a unit failure
(fleet-ops#3121, Nish 2026-09-04 correction: devin / free-glm / senior
three-role panel stays).

### What changed

- **config/seat-caps.json** — add `senior_seats_in_order`
  (`cursor/cursor-grok-4.6-high`, `xai-oauth/grok-4.6`,
  `openrouter/deepseek/deepseek-v4-flash-0731`) and **delete**
  `keystone_only_providers` — one mechanism, not two (cursor stays
  keystone/senior-review via the hardcoded gate in seat-lib).
- **lib/seat-lib.sh** — parse `senior_seats_in_order` into `SEAT_SENIOR_ORDER`
  and add `find_senior_seat()`: first usable seat in the ladder wins; if the
  whole ladder is walled it falls through to any usable capable seat
  (a walled role resolves to its fallback instead of failing the unit). EOF:
  returns 1 only when nothing anywhere is usable right now (lane fault).
- **bin/pi-audit-run** — rename the `straitly` role to `senior` (keep
  `straitly` as a back-compat alias) and resolve it via the ladder; add the
  `free-glm` role (zenmux/z-ai/glm-5.3-free, cline/z-ai/glm-5.3-flash, then
  any free-class seat, then any capable seat). No money-wall fail-loop: when
  every senior seat is walled, the role rotates rather than exits 1.
- **bin/fleet-heartbeat-auditor** — panel `ROLES=(devin free-glm senior)`;
  add the **owner-authored admission bypass**: a `scout-candidate` authored
  by `nish3451` whose body passes `lib/agent-ready-spec-gate.py` goes
  straight to `agent-ready` (no three-seat panel), one `OWNER-BYPASS` log
  line each.
- **bin/pi-escalation-audit** — panel `ROLES=(devin free-glm senior)`.
- **bin/fleet-gap-closure-conference** — resolve the third auditor
  (`DEFAULT_AUDITORS`) to the first-usable senior seat at start instead of
  the hardcoded dead `straitly`/ds4-pro.
- **bin/fleet-blind-audit** — the judge/reviewer seat prefers
  `find_senior_seat()` (senior ladder) and falls back to `pick_seat`.
- **bin/pi-audit-tally** — reject a `2-of-3 PASS` admission when any PASS
  reason is keyword-only with no repo-specific evidence (an issue number
  `#N`, a path, or a URL); `EVIDENCE-REFUSED`, no `agent-ready`. Keyword-only
  compliance is not a judgement.

### Verification

Execution IS the review — the deliverable (the panel seat resolution, the
owner bypass, and the tally evidence gate) is exercised by the repo's own
retest suite to a green run:

```
bash tests/pi-audit-run.test.sh            -> PASS (senior ladder falls through walled cursor to xai-oauth/grok-4.6)
bash tests/fleet-heartbeat-auditor.test.sh -> PASS (scenario14 owner-bypass nish3451 -> agent-ready;
                                               scenario4c EVIDENCE-REFUSED keyword-only reason refused)
bash tests/pi-escalation-audit.test.sh     -> PASS
bash tests/seat-lib.test.sh                -> PASS (find_senior_seat + cursor weekly ceiling, no keystone regress)
bash tests/fleet-gap-closure-loop.test.sh  -> PASS (conference senior seat)
bash tests/fleet-token-economy.test.sh     -> PASS (senior_seats_in_order replaces keystone_only_providers)
bash tests/token-economy-routing.test.sh   -> PASS (cursor keystone-only, senior-review routing)
python3 lib/rule-enforcement.py validate-matrix --matrix config/rule-enforcement.json -> OK (128 rules)
sgscan --base origin/main                  -> No new security findings
./bin/fleet-no-agent-names-check --pr-body .pr-body-3121.md --commit-range origin/main..HEAD -> OK
./bin/fleet-token-efficiency-check --name-status <(git diff --name-status origin/main...HEAD) -> OK
./bin/fleet-exec-review-canary --body .pr-body-3121.md -> OK
./bin/prove-one-run-check --body .pr-body-3121.md --name-status <(git diff --name-status origin/main...HEAD) -> SKIP (no new unit/timer/workflow)
./bin/fleet-organ-heartbeat-check gate ... -> SKIP (no fleet organ touched)
./bin/fleet-wipe-lessons-check scan --root . -> clean
```

Live run of the deliverable (the senior seat resolver, against real seat
ledger state on this host):

```
# configured ladder loads in order and resolves to the first usable seat
source lib/seat-lib.sh; SEAT_CAPS_JSON=config/seat-caps.json; load_seat_caps
$ find_senior_seat
ORDER=[cursor/cursor-grok-4.6-high xai-oauth/grok-4.6 openrouter/deepseek/deepseek-v4-flash-0731]
CEILING=300
find_senior_seat rc=0 seat=[cursor	cursor-grok-4.6-high]

# empty/walled ladder falls through to any capable seat (no money-wall fail-loop)
$ find_senior_seat   # live cfg has no senior key yet (pre-merge deploy)
find_senior_seat: senior ladder exhausted/walled; falling through to any capable seat
find_senior_seat rc=0 seat=[openrouter	deepseek/deepseek-v4-flash-0731]
```

Cursor (the ladder head) resolves first while its weekly prepaid-usage meter
is under the 300 ceiling; a walled/benched ladder never stalls a unit — it
rotates to the next usable capable seat instead (fleet-ops#3121 no-fail-loop).

### Follow-ups filed separately

- `fleet_audit_votes_total{role,verdict}` and
  `fleet_scout_candidate_age_p50_s` metrics do not yet exist in
  `libexec/fleet-metrics-export.py` — filed as #3296.
- The one-pass 0509 `scout-stale` (>7d, no PASS) drain is operational —
  filed as #3297.

This PR delivers the code that unblocks the panel itself; the two follow-ups
carry the observability tail and the drain pass so nothing is lost.

### Pre-existing live-state finding

On this host a live `fable-fleet-check.timer` exists but is not tracked in
`MANIFEST` (grep count for `fable-fleet-check` in `MANIFEST` = 0). This is
unrelated to the audit-panel changes — filed separately as #3336.

Closes #3121
Relates to #3336