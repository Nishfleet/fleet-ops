# Plan: fleet-ops#5692 — orphan Actions registration self-scan

Note: stock planner subagent returned no output (twice); manager wrote the plan directly. One-line reason: planner tool silent-fail.

- [x] phase 1: `bin/fleet-workflow-registration-scan` — list ACTIVE Actions registrations via `gh api repos/<repo>/actions/workflows`, diff against `.github/workflows/**` on origin/main fetched to a temp ref (no clean checkout required), report to /home/nish/workspaces/agent-state/FLEET-ORPHAN-WORKFLOW-REGISTRATIONS.md, exit 1 on orphan / 0 clean, `--report` always exits 0
- [x] phase 2: wire the scanner as a fleet-heartbeat-tier1 canary step (bin-side only, no `.github/workflows/**` edits) so it lands in the tier-1 rc summary
- [x] phase 3: `tests/workflow-registration-inventory.test.sh` — (a) mocked-API synthetic orphan flagged, (b) clean inventory exits 0 — registered in the P14 test listing (#3482/#3483 convention)
- [ ] phase 4: file the #1161-format orphan report issue for the standing-rules-drift registration with the one-command admin remediation runbook (deletion is irreversible + admin-gated; worker must not attempt Actions settings mutations)
- [ ] phase 5: verify per issue verify block (scan --report grep standing-rules-drift.yml), reviewer round, PR + arm
