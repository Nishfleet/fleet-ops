## Summary

Closes #5807: the merge-queue head wait and the hosted-CI (GitHub Actions) slot depth were blind. 2026-09-12 10:50 IST: the 0509 queue head #3054 sat AWAITING_CHECKS 2h20m with 14 entries behind it, 58 queued + 11 in-progress hosted runs against the ~20-concurrent org plan, 0 merges in the last hour — and no alert (only the unrelated FleetMainRed); a human noticed by hand.

Fix, entirely inside organs that already exist — no new unit, no new timer, no faster polling:

- `libexec/fleet-metrics-export.py` (the EXISTING 5-min exporter) publishes 4 gauges per enrolled repo: `ci_merge_queue_head_wait_seconds`, `ci_merge_queue_entries`, `ci_hosted_runs_queued`, `ci_hosted_runs_in_progress`. ONE aliased GraphQL call covers every enrolled repo's mergeQueue (never one call per repo); the hosted-run counts are two tiny REST `total_count` reads per repo (~5 calls per 5-min tick ≈ 60/hr against the 5000/hr shared App-install window ≈ 1.2%, fleet-ops#5762). When the #1350 20% throttle is breached the read is SKIPPED and the stale cache serves — the detectors never cost the fleet its rate-limit headroom. A failed read serves its cache up to 2h, then the family OMITS (never a frozen value) so the alerts' `absent()` legs speak for a dead read path (fleet-ops#1010). The stored shape keeps `head_enqueued_at`, so a ≤10-min-old cache still emits a current wait when aged against the serve-time clock.
- `config/fleet_rules.yml` gains the two acceptance rules, both routed to the existing repair-dispatch receiver (neither is a Nish page): `CiMergeQueueHeadWaitHigh` (critical, >1200s for 10m, `absent()` leg) whose description carries the repair worker's runbook — the head PR's waiting checks, then the repo's queued+in-progress runs grouped by workflow with count, and the consolidation pointers 0509#3069/#3068/#3070 (never a new workflow — PR-only) — and `CiHostedQueueDepthHigh` (warning, >30 queued for 15m, `absent()` leg) with the same slot-consumer runbook. Comments cite the incident, the #1010 absent() rationale and the #5762/#1350 budget.
- `libexec/fleet-console-pi/generate.py` + `shell.html`: the MAIN-RED tiles gain an overlay-only second line — head wait (h/m/s), queue depth, runs queued, in flight — read in ONE PromQL instant query; a repo without the families renders exactly as before (fail-open, the tile never blanks).
- Tests: `tests/ci-merge-queue-detector.test.sh` (511 lines, hermetic — no network/gh/Prometheus; promtool legs self-skip when promtool is absent): the 10:50 IST evidence through the real functions, both rules' thresholds/for/severity/absent/SKIP_SET/consolidation-refs pinned, promtool firing BOTH (with a quiet control), the 25-min-head-wait drill through the REAL alert-repair-dispatch (mocked `pi-systemd-run`) dispatching exactly one repair packet and then clearing, the four families surviving `main()` end-to-end, and the #5762 throttle gate (throttled → no gh read, stale cache serves). Hosted via one line in `tests/ci-standards-audit.test.sh` (the worker App cannot push `.github/workflows/**`), hard-pinned by name in `tests/p14-test-listing-gate.test.sh` (the #5870/#6028 lesson).

Closes #5807

net-positive-because: zero new machinery — the gauges ride the existing 5-min exporter (MERGE_QUEUE_TTL 240s < the 300s tick, so every tick is eligible to refresh and the cadence never rises), the alerts reuse the existing repair-dispatch receiver and the existing #1010 absent() convention, the console change is overlay-only, and the repair runbook points at the consolidation issues that already exist instead of proposing new ones.

## Verification

- `bash tests/ci-merge-queue-detector.test.sh` → exit 0, all six legs: `OK: exporter publishes the 4 gauges; 10:50 IST evidence parses to 8400s/14/58/11` · `OK: both rules pinned (thresholds/for/severity/absent/SKIP_SET/consolidation-refs)` · `OK: promtool: 10:50 IST snapshot fires BOTH; recovered values clear; quiet control silent` · `OK: drill: 25-min head wait dispatched exactly one packet + worker, consolidation issues + slot one-liner aboard` · `OK: drill: cleared — resolved notification dispatched nothing (SKIP resolved, 1 packet, 1 DISPATCH total)` · `OK: main() end-to-end writes the 4 families (10:50 values); #5762 throttle serves stale, reads nothing`.
- `bash tests/p14-test-listing-gate.test.sh` → exit 0, ends `OK: p14-test-listing-gate.test.sh: P14 test list is closed`, including this PR's own pin: `OK: ci-merge-queue-detector.test.sh host line in ci-standards-audit.test.sh is pinned (fleet-ops#5807)`.
- `bash tests/fleet-metrics-export.test.sh` → exit 0 (nested-suite battery; the exporter file this PR extends, including #6419's same-day amendments). The full hosted `ci-standards-audit.test.sh` was deliberately NOT run in this worker: the memory-budget rule (fleet-ops#4891) keeps one heavy toolchain process at a time and the PR CI round-trip adjudicates the full suite. Known, not this PR's: the #3310 / wd-hang-3883 / help-flag legs flake on LiteLLM readiness (each solo-green; the #6343/#6342 class).
- `sgscan` → `No new security findings.`
- `bin/fleet-organ-heartbeat-check gate --name-status <(git diff --name-status origin/main..HEAD) --rules-diff <(git diff origin/main..HEAD -- config/fleet_rules.yml)` → `OK: organ-heartbeat invariant holds (touched organs: gh-rate-limit, metrics-export; new candidates: none; registry touched: False)` — the absent() legs ride this same diff (fleet-ops#1010).
- `bin/fleet-token-efficiency-check --name-status <(git diff --name-status origin/main..HEAD)` → `OK: no token-efficiency anti-patterns in changed prompt assemblers`.
- `bin/fleet-no-agent-names-check --pr-body .fleet/pr-body-5807.md --commit-range origin/main..HEAD` → exit 0 (see gate block below).
- Salvage note: this PR continues this unit's own prior runs (heartbeat released the claim repeatedly, StartLimitBurst twice, before one run committed the implementation but died before opening a PR — see the issue's comment trail); nothing was re-derived: the salvaged implementation was rebased onto 2026-09-13 13:48Z `main` (`bda7296dd`) and every test above re-run on this worktree rather than trusted. The predecessor PR #6285 (same work on an older base, head `51c1a21ed`) was closed unmerged when the heartbeat reset the claim branch — this PR supersedes it; the diff is issue-scoped (8 files, +968/−2).

## run-proof

units: pi-issue@fleet-ops-5807.service (this run — cgroup-verified `/user.slice/user-1000.slice/user@1000.service/app.slice/app-pi\x2dissue.slice/pi-issue@fleet-ops-5807.service`, 2026-09-13 19:47–19:53 IST). timers: the CARRIER, `fleet-metrics-export.timer` (description: "Run fleet-metrics-export every 5 minutes"), proven live on this host — last fired Sun 2026-09-13 19:50:04 IST, 3m17s prior at read time (19:53:21); this diff touches no `systemd/` file (`git diff --name-only origin/main..HEAD -- systemd/` → 0), so there is no new timer to prove. The 4 gauges + 2 rules + console overlay ship via the merge+deploy of this branch (the installed exporter here still predates them — nothing in production is claimed); the acceptance's drill dispatch is proven synthetically in the test (real alert-repair-dispatch, mocked `pi-systemd-run`, 25-min head wait → exactly one packet, then clears on the resolved notification).

## Test plan

- `bash tests/ci-merge-queue-detector.test.sh`
- `bash tests/p14-test-listing-gate.test.sh`
- `bash tests/fleet-metrics-export.test.sh` (the exporter this PR extends, incl. #6419's amendments)

loose-ends: ci-mq-first-live-serve — after merge+deploy, the FIRST post-merge exporter run should be spot-checked to emit the four families in the `fleet.prom` textfile (they are test-proven, not yet live-served), and the next real head-wait >20m incident (or a forced 1500s cache) should be watched through alertmanager → repair-dispatch → packet, closing the loop the test only mocks. Known, not this PR's: the #3310 / wd-hang-3883 / help-flag audit legs flake on LiteLLM readiness (each solo-green; the #6343/#6342 class). Separately observed while working (mechanism verified, working as designed, no issue existed): `fleet-merged-pr-close.service` sits red because `fleet-dead-pr-detector` exits 1 by design on any dead/stale-conflicting finding (bin lines 443-446; the unit's ExecStartPre deliberately has no `-`) and PR #5834's conflict vs its OPEN parent #5792 crossed the 24h stale bound (25h) — it stays red, and OnFailure re-escalates, until #5834 is landed/rebased/closed. Fleet-wipe-lessons: worktree `issue-fleet-ops-5807` removed at close.
