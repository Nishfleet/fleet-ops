# Handoff note (fallback when PR body missing)

State: Worker completed the issue; PR body was empty or missing.
Next step: Verify the PR was opened and has the verification receipt; arm auto-merge.
Blockers: None.
Proof refs: This fallback note is used only when the worker left no PR body at all — check the journal and session for the real trace.