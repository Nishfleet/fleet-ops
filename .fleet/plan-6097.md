# Plan: fleet-ops#6097 — escalation admission carries the #543 spec clauses + fail-closed agent-ready spec gate (finish and ship)

## Context

- Issue: Nishfleet/fleet-ops#6097 — the senior-auditor escalation tally must (a) compose the admitted fix-issue body with the #543 spec clauses (`accept:` / `metric:` / `moves: product_merges_per_day`) and (b) run `lib/agent-ready-spec-gate.py check-body` FAIL-CLOSED before applying the `agent-ready` label — refusal means the fix issue still files WITHOUT the label, LOUD `[SPEC-GATE]` on stderr.
- Worktree: `/home/nish/workspaces/agent-worktrees/issue-fleet-ops-6097`, branch `claim/issue-6097`, one commit ahead of `origin/main`: salvage commit `42213dd2f` "wip(salvage): pi-issue-fleet-ops-6097 success/0" — the complete intended 3-file diff. Leave it untouched unless a test fails.
- Escalated check: workflow "CI" → job "P14 tests / PR checks" (ci.yml `tests` → reusable-pr-checks.yml) → step "Verify (test + build)", event `pull_request`. Prior repair #6095 merged 2026-09-12T21:40:33Z — that ends the failure window; green must come from a fresh post-repair run, not a re-run inside the window.
- The 2-of-3 panel verdict reason is already logged as an issue comment by the prior claim; the remaining shipped work is this diff. Exactly ONE implementation phase remains plus the ship step. Finish-and-ship, not a build.

### Phase 1 — finish and prove (worker)

- [x] phase 1: acceptance 1 (prove half) — in this worktree, run `bash tests/pi-escalation-audit.test.sh` and `bash tests/agent-ready-spec-gate.test.sh` to green; then sgscan over the diff: `bin/sgscan --base origin/main` (repo-documented invocation). If the sgscan binary is absent, record the gap — do not install anything. Do NOT amend/rewrite the salvage commit's 3 files unless a test fails (then fix the failing file and amend `42213dd2f` so `origin/main...HEAD` stays exactly the 3-file diff).

### Phase 2 — ship (manager-owned)

- [ ] phase 2: acceptance 1 (ship half) — receipts into the PR body (#6097, the #543 clauses, green test + sgscan outputs, the escalated-check identification, the prior-claim verdict comment), then `gh pr create` from `claim/issue-6097`; the verdict work is shipped, not dismissed. Confirm the escalated check — workflow "CI" → job "P14 tests / PR checks" → step "Verify (test + build)", event `pull_request` — is green on the PR's own fresh post-repair run, not a re-run inside the failure window (window closed at the #6095 merge, 2026-09-12T21:40:33Z); then arm auto-merge (agent-authored PRs self-land).
- [ ] phase 2: acceptance 2 — metric: zero occurrences of the escalated check's PR-plane failure signature after the repair merges. The two known single-branch lane-local reds (#5888, #6217) passed on other branches at the same times — they are not the base-red signature and don't count.
- [ ] phase 2: acceptance 3 — moves: product_merges_per_day; this PR's landing is the counted move.

## Review

Phase-1 diff for the reviewer at prove time: `git diff origin/main...HEAD` — exactly the salvage commit's 3 files (`42213dd2f`):

- `bin/pi-escalation-audit-tally` — the admitted fix-issue body now composes the #543 clauses (`accept:` / `metric:` / `moves: product_merges_per_day` under `## Spec (fleet-ops#543)`); `lib/agent-ready-spec-gate.py check-body` runs FAIL-CLOSED before the label (pass → `--label agent-ready`; refusal → fix issue still filed WITHOUT the label, LOUD `[SPEC-GATE]` on stderr); `AUDIT_SPEC_GATE` env override for tests.
- `lib/agent-ready-spec-gate.py` — `FIRST_ADMISSION` registers `bin/pi-escalation-audit-tally` (the gate runs fail-closed before the label).
- `tests/pi-escalation-audit.test.sh` — scenario 2 now asserts all three clauses in the filed body; new scenario 2b: stubbed refusing gate → fix issue filed WITHOUT `--label agent-ready`, LOUD `[SPEC-GATE]` on stderr.

Notes: `.fleet/plan-6097.md` is plan-only; prior plans (e.g. `.fleet/plan-5792.md`) are tracked on main, so it may ride this PR (making `origin/main...HEAD` 4 files) or be committed separately — the three acceptance files above are what phase 1 proves and what the reviewer diffs. `.fleet/plan.md` (issue #5141, stale) stays untouched.
