# Plan: fleet-ops#5792 — senior-lane router retry/cooldown rescue (manager mode, managed by pi-issue-fleet-ops-5792)

Scope: PR on `claim/issue-5792` in this worktree. Router retry/cooldown/roster numbers already fixed by merged PR #5811 — do NOT touch `config/litellm-proxy.yaml`. Seat-caps alignment is open PR #5834 — out of scope. Remaining: docs + offline replay test.

- [x] phase 1: docs `docs/design/senior-lane-router.md` — senior ladder (pareto glm-5.3 order:1 prepaid, xkiro deepseek-v4-pro order:1 free, synthetic glm-5.3-flash order:2 prepaid), fallbacks matrix, retry_policy/allowed_fails=1/cooldown_time=300, the guarantee that one 403 spending-limit/402 insufficient-credits deployment is benched by allowed_fails_policy.AuthenticationErrorAllowedFails=0 + cooldown and never reaches a worker; cross-link docs/design/litellm-vs-seat-lib.md and #5792; state "retry/cooldown/roster landed via PR #5811 (2026-09-12), seat-caps via PR #5834"
- [x] phase 2: test `tests/forced-bad-deployment-replay.test.sh` — offline, pattern of tests/seat-credentials-bad-replay.test.sh (ok()/fail(), scratch dir, set -euo pipefail); parse config/litellm-proxy.yaml with python3+yaml (fail loud if missing); assert router_settings.num_retries>=2, allowed_fails<=1, cooldown_time>=300, AuthenticationErrorRetries>0, AuthenticationErrorAllowedFails=0, every model group's fallback chain terminates at an existing group (no dead-end)
- [x] phase 3: in-test forced-bad-deployment simulation on an in-memory copy: inject a dead deployment (api_base https://127.0.0.1:1) FIRST in the senior group (order:1, earlier in list order than pareto) and assert the policy numbers mean one failure benches it (allowed_fails=1, cooldown 300) while the remaining senior rungs + fallbacks senior->worker-capable keep serving; also negative check: a config copy with allowed_fails=5 must FAIL the assertion (detector catches drift)
- [x] phase 4: run green locally: bash tests/forced-bad-deployment-replay.test.sh, bash tests/seat-credentials-bad-replay.test.sh, bash tests/agent-cron-fable-check-litellm-routing.test.sh; and any adjacent litellm tests still green
- [ ] phase 5: diff gate: git diff origin/main..HEAD touches ONLY the two new files (config/litellm-proxy.yaml untouched); commit, push, PR; manager opens PR and arms auto-merge

Notes: seat-caps half of #5792 is open PR #5834 (not this PR — Relates). Money decision (Nish, 2026-09-12): prepaid Grok/OpenRouter rungs benched in yaml by #5811; docs record that history, do not re-decide it.

--- reviewer round (senior reviewer agent, phase diff docs+test) — PASS, no Act-on
Consider (recorded, not re-delegated): test at ~line 52 hardcodes fallback matrix — add comment linking doc/config/test copies; python block at line 104 interpolates yaml path into source — prefer sys.argv; doc "source of truth on main" vs worktree copy — drill-test acceptable.
Noted: simulation is policy-arithmetic offline by acceptance design; senior<->worker-capable fallback cycle terminates (litellm skips tried groups).
