# Plan — fleet-ops#6034: pi-intake-repair@* 1800s timeout + StartLimitBurst=2 latch

Branch claim/issue-6034, this worktree. Both `pi-intake-repair@{0509,fleet-ops}` units (OnFailure-triggered by `pi-intake@<repo>.service`) die at `TimeoutStartSec=1800`; the `StartLimitIntervalSec=21600`/`StartLimitBurst=2` latch then fails both units and every ~61s retrigger re-trips `OnFailure=unit-escalation` — the runs are slow-working, not stuck, so the fix is a 2700s cap plus a wall-clock budget stamp in the run log.

- [x] phase 1: (accept 1) capture the journal root cause into `.fleet/evidence-6034.md` BEFORE any unit edit — evidence: morning run 08:25:17 start → 08:54:30 Finished class=worked (29m13s, tools=10); evening runs 18:52:55 (0509) + 18:53:20 (fleet-ops); 18:54:17 BOTH lanes log `SPAWN_BLOCKED reason=home_wide_filesystem_sweep` in the same second (that guard is repo-owned: template/extensions/spawn-guard-core.ts, fleet-ops#4896 — it blocks the agent's own `find`/`rg -r` rooted at ~ or /home/nish, a 282 GB home dir, historically load-20); the evening fleet-ops run executed 53 tools and was killed at the 1800s cap; 19:23:20 prints "start operation timed out. Terminating." AND "PACKET-VERDICT tools=53 class=worked" AND "Failed with result 'timeout'" in the SAME second (the packet-verdict pi extension prints its verdict during the SIGTERM unwind — benign but misleading, the session never finished); counter-evidence 20:10:23/29 manual reset+start → BOTH units Finished class=worked at 20:28:45/20:30:46 (18m22s/20m17s, tools=40/43); conclusion: slow-working, NOT stuck (tools kept accumulating, morning finished inside the cap, post-reset runs finished 18-20m) → 2700s is the right lever, no re-scope needed.
- [ ] phase 2: (accept 2) exactly ONE unit change — `TimeoutStartSec=1800` → `TimeoutStartSec=2700` in `systemd/pi-intake-repair@.service`; `StartLimitIntervalSec=21600` and `StartLimitBurst=2` UNCHANGED (fleet-ops#5036: raising the burst re-wedges); no OnFailure, timer, ExecStart, or credential edits; NO .github/** or other gate-owned files; extend `tests/pi-intake-repair-run.test.sh` to assert the unit now carries 2700 AND that the StartLimit lines are byte-identical, keeping the existing assertions (seat rotation, no-hardcode, MANIFEST, escalation-canary, systemd-analyze verify) intact.
- [ ] phase 3: (accept 3) prevention mechanism — the repair run's PACKET-VERDICT line gains its wall-clock budget, emitted by `bin/pi-intake-repair-run` (not a new checker). Shape: stream pi's stdout through `tee` into a temp capture so the journal keeps today's live verdict even on a timeout kill (tee passes bytes through as they arrive — no buffering regression); after the pipeline exits, if the capture contains a `PACKET-VERDICT` line, echo that same line with ` elapsed=Ns budget=Ns` appended (SECONDS-based elapsed; example: `PACKET-VERDICT tools=53 class=worked elapsed=1798s budget=2700s`); if no verdict line exists (fast provider death), print nothing extra (no synthetic line); the budget comes from the LIVE unit via `SYSTEMCTL`-overrideable `systemctl --user show pi-intake-repair@<repo>.service -p TimeoutStartUSec --value` (prints "45min" for 2700 — parse *min→*60s, *ms→/1000, *s→as-is; unparseable/missing → omit the budget= field, never fabricate); the SYSTEMCTL env override mirrors install.sh's precedent so the test can stub it; preserve exit semantics (wrapper exits with pi's rc; the killed path keeps today's exact behavior — unaugmented verdict + "Failed with result 'timeout'"); update `tests/pi-intake-repair-run.test.sh` for the new final-line shape (stub SYSTEMCTL to a tiny script printing "TimeoutStartUSec=45min").
- [ ] phase 4: (accept 4) prove + land — inner loop (Execution IS the review) to green on the touched tests, run the repo's focused bash tests + sgscan (NO vitest/coverage/typecheck), then the manager pushes claim/issue-6034, opens the PR (body: phase-1 evidence + Verification + run-proof + loose-ends) and arms auto-merge; post-merge the standard fleet-ops-deploy (merge-to-live, runs on the existing fleet-deploy-check timer) pulls main into the deploy clone — BOTH installed artifacts are MANIFEST symlinks into that clone (`/home/nish/.config/systemd/user/pi-intake-repair@.service` and `/home/nish/.local/bin/pi-intake-repair-run`) — install.sh runs the user-scope install + daemon-reload; then the issue's termination check must pass: `grep -q "TimeoutStartSec=2700" /home/nish/.config/systemd/user/pi-intake-repair@.service` (symlink reads through) AND no failed pi-intake-repair@* units; the 6h StartLimit window + 24h recurrence watch are observe-to-close via the existing escalation/heartbeat machinery (any recurrence re-opens this as an incident).
