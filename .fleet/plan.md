# Plan — Nishfleet/fleet-ops#5003: product-outcome tile (display + verify contract only)

- [x] phase 1: `libexec/fleet-console-pi/generate.py` — add `collect_outcome()` after `collect_shipped()` and register `doc["tiles"]["outcome"] = collect_outcome()` directly after the `shipped_24h` line. Gate on BOTH existing freshness sources (`_prom_or_stale` for fleet.prom, which carries `fleet_signups_7d`; `_product_slo_mtime()` for fleet-product-slo.prom, which carries the three `fleet_product_*` gauges). Read exactly the four EXISTING gauges with `_prom_query`. If ANY gauge returns no rows, the tile is `_unknown(...)` with NO numeric field — an absent gauge can never be coerced to 0. No new exporter, timer, service, cache file or D1 read. (accept 1, 2)
- [x] phase 2: `libexec/fleet-console-pi/verify.py` — add an `outcome` SPEC in the existing `field`/`tolerance` shape and a `run_outcome_prom` runner that recomputes ALL FOUR numbers from the same PromQL with the same freshness gate, so a lie is DISPUTED and exports `fleet_console_tile_mismatch{tile="outcome"} 1` for the EXISTING ConsoleLying rule. No new rule. (accept 4)
- [x] phase 3: `libexec/fleet-console-pi/shell.html` — one new `section-outcome` immediately after `section-shipped`; headline = signups 24h, secondary = 7d / activated / paying / the explicit `top of funnel UNMEASURED (no visit/page-view gauge exists; Nishfleet/0509#2120)` line. Not-live renders the reason only, never digits. Existing sections keep their relative order; `shipped_24h`'s number and label are untouched. (accept 3, 5)
- [x] phase 4: `tests/console-tile-verify.test.sh` — scenario (a) the outcome tile is present with all four numbers verified by the REAL runner, (b) an injected lie on `signups_7d` goes DISPUTED with `fleet_console_tile_mismatch{tile="outcome"} 1`, (c) the shell section exists with the four labels and the UNMEASURED funnel line. (accept 6)
- [ ] phase 5: prove one clean run (console suite, sibling console suites, drill `--check`, live `generate.py`), confirm no new alert/timer/service/MANIFEST line, then open the PR and arm auto-merge. (accept 7)

## Manager amendments
- Phase 3 carries an integration fix found in the phase 1/2 review: the tile's freshness anchor is the product-slo mtime, not `min(fleet.prom, product-slo)`. `verify.py:_race_against_tile` compares against the product-slo mtime only, so the older fleet.prom anchor made the shared race gate fire on every tick and a lying outcome tile SKIPPED instead of DISPUTING. Both sources are still gated; only the anchor changed.
- Phase 2 retry (the one allowed retry) after review: (A) an absent gauge is no longer coerced to 0 inside the outcome runner, (B) a non-finite sample fails closed to DISPUTED (verify) / `_unknown` (generate) instead of aborting the whole verify pass.
- Consider, recorded not re-delegated: the outcome runner gates only the product-slo textfile for the race, so a fleet.prom-only rewrite between generate and verify could false-DISPUTE the 7d field; there is no `spot` fallback for the outcome tile (a race is unverified for that cycle, not deferred); `_promql_sum_present` stays outcome-scoped rather than shared. File as follow-ups if they recur.

## Phase review record (manager, per-phase reviewer)
- Phases 1-2 review (stock reviewer, patch `/tmp/phase12-5003.patch`): **1 BLOCKING + 3 ACT-ON + CONSIDER**. Blocking: the `min(mtime, pmtime)` freshness anchor guaranteed the shared race gate fired on every tick, so a lying outcome tile was silently SKIPPED and `fleet_console_tile_mismatch{tile="outcome"}` was permanently 0 — proven against live Prometheus (fleet.prom 21s older than product-slo). Already fixed by phase 3 (`observed_at=pmtime`); regression-locked in the test suite. ACT-ON: (A) absent gauge coerced to 0 in the verifier (a fake-zero tile verified as truthful), (B) uncaught `ValueError`/`OverflowError` on a non-finite sample aborted the entire verify pass, (C) only one of the two freshness anchors is race-checked. (A) and (B) fixed in the phase 2 retry; (C) recorded in the amendments above.
