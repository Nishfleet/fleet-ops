# fleet-ops#5079 — dead-PR detector: stop reading clean over UNKNOWN mergeable

- [ ] phase 1: In `bin/fleet-dead-pr-detector`, treat anything that is not exactly `MERGEABLE` or `CONFLICTING` (the string `"UNKNOWN"`, `null`, or any future value) as unknown: count it in the `unknown-mergeable` figure (line ~180) and do not silently drop it from the scan (the `select(.mergeable == "CONFLICTING")` filter at line ~219 must keep unknown PRs in the loop).
- [ ] phase 1: For each unknown-mergeable PR, force a computation before concluding: re-query that PR with `gh pr view <n> -R "$REPO" --json mergeable` (up to 3 attempts, short sleep between); only a `MERGEABLE`/`CONFLICTING` answer may end the question.
- [ ] phase 1: If a PR's mergeable is still unknown after the retries, resolve its parent first and decide by parent state so the sweep can never read clean over a possible dead PR: parent CLOSED/MERGED -> `loud "DEAD-PR-UNRESOLVED"` + `exit 2` (fail closed, this is the page); parent OPEN -> log the unknown and continue (no page: a live parent is never dead); keep the three documented exit codes (0/1/2), do not invent a fourth.
- [ ] phase 1: Make the honesty line truthful: `scan done: conflicting=<n> unknown-mergeable=<n> dead=<n>` must count every non-`MERGEABLE`/non-`CONFLICTING` value, so a blind tick is visible in the journal even when nothing else fires.
- [ ] phase 2: Add a case to `tests/fleet-dead-pr-detector.test.sh` (extend the `gh` shim with `pr view`, which it does not serve today) that mocks `gh pr list` returning `"mergeable":"UNKNOWN"` for a PR whose parent issue fixture is `CLOSED`, and asserts the detector NEVER exits 0 with `dead_conflicting_prs=0` — it must either classify it dead (`rc=1`, `parent-state=CLOSED` in the evidence line after the `gh pr view` re-query succeeds) or fail loud (`rc=2`); keep all existing cases green, including case 6 (cross-repo ref -> skipped, exit 0): an unknown value must not resurrect the bogus-parent class.
- [ ] phase 3: No new organ, no new timer, no new unit: extend only the existing script, its existing test file, and its existing rail (`ExecStartPre` of `fleet-merged-pr-close.service`); no `migrations/**` change — verify with `git diff --name-only` that the diff is exactly `bin/fleet-dead-pr-detector` + `tests/fleet-dead-pr-detector.test.sh`.
- [ ] phase 3: Do not touch gate-owned paths: `.github/workflows/**`, `.github/scripts/**`, `CODEOWNERS`, `.gitleaksignore`, `.gitleaks.toml`, `.semgrepignore`, `.semgrep.yml`/`.semgrep.yaml`; must not remove or skip any test — run `bash tests/fleet-dead-pr-detector.test.sh` and confirm all cases pass (14 existing + new UNKNOWN case).
- [ ] phase 4: Post-merge follow-up: confirm the next live tick of `fleet-merged-pr-close.service` flags PR #4978 (parent #4945 CLOSED); record its disposition as rebase-and-land in a comment on issue #5079.
