# p14-main live proof — fleet-ops#6159 (unit pi-issue-fleet-ops-6159)

Run the CLAIM BRANCH's measure.sh (worktree /home/nish/workspaces/agent-worktrees/issue-fleet-ops-6159)
once, unbounded (its own 2100s budget), against the real origin/main:

    cd /home/nish/workspaces/agent-worktrees/issue-fleet-ops-6159 && bash measure.sh

The detector (lib/measure-p14-main.sh, wired at the measure.sh tail) must print
exactly one p14-main(main=<12hex>): verdict line. That line, and only that line,
is the deliverable:

    grep -E '^p14-main' the-output > /home/nish/workspaces/agent-worktrees/issue-fleet-ops-6159/.fleet/p14-main-6159.deliverable

Empty deliverable = failure (the detector prints its line on every path, so a
missing line means the wiring is broken). The verdict lands in the shared cache
(~/workspaces/agent-state/measure/p14-main/<sha>) where the judge's later reads
become instant cache hits. UNAVAILABLE:timeout(after=N/109) is an honest
verdict, not a failure of this drill.
