# Plan for #7479 (light)

- [ ] phase 1: keep / finish the salvaged patch: cap warns with count + signal names and returns 0; tier1 propagates real failures (rc=1 too); verify with signal-reconcile tests.
- [ ] phase 2: metrics exporter emits `fleet_signal_reconcile_capped` from the reconciler's JSON snapshot; verify with a metrics-exporter run against a fixture snapshot.
- [ ] phase 3: heartbeat proof path: test that a capped reconciler still reaches the dead-man ping and an exception does not; repo tests + sgscan; PR with run-proof.
