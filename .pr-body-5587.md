Closes #5587

## Summary
- Deletes the hard-coded live-state count "(-26 user timers)" from the canonical AGENTS.md source at `lib/pi-agents-md/canonical.md`. The two-check rule (FLEET-PAUSED marker, then `systemctl --user list-timers`) already carries the truth; the count had drifted (26 -> 54) and its whole section forbids rotted state claims.
- Both renders regenerated with `bin/render-pi-agents-md.py --render`, which reported `OK (checked): 2 target(s) match canonical`; `grep -c "~26 user timers"` on both `/home/nish/AGENTS.md` and `/home/nish/.pi/agent/AGENTS.md` now returns 0.

## Verification
- Live check before edit: `XDG_RUNTIME_DIR=/run/user/$(id -u) systemctl --user list-timers --no-legend | wc -l` -> 54 (issue evidence confirmed).
- After edit: no occurrence of `~26 user timers` in canonical.md or either render; render script `--render` plus its built-in check passes.

## run-proof
- run-proof: `bin/render-pi-agents-md.py --render` run in the worktree; output "rendered /home/nish/AGENTS.md", "rendered /home/nish/.pi/agent/AGENTS.md", "OK (checked): 2 target(s) match canonical" (manual one-shot command, no unit/timer needed).
- organ-heartbeat: lib/pi-agents-md/canonical.md not-an-organ: static docs source rendered by an idempotent script, no heartbeat state.
- loose-ends: none — single-line docs removal; no new bin/ files (research/help-first not applicable).

## Test plan
- `bin/render-pi-agents-md.py --render` then `--check` path passes (embedded OK line).
