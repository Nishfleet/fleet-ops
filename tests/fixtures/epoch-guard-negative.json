{
  "_comment": "Negative fixture for tests/hardcoded-epoch-guard.test.sh (fleet-ops#4508). The reset field below carries the exact time-bomb literal that red'd the P14 fleet-metrics-export step on every PR once the calendar rolled past it (2026-09-08T08:00:00Z). Excluded from the guard's default tree scan by the epoch-guard-*.json basename rule; the test proves the guard fires on it by running with a frozen now before that instant. Do NOT remove and do NOT make the value relative - it is the static proof that the guard catches the class.",
  "reset": 1788854400
}
