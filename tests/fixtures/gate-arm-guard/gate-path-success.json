{
  "name": "gate-path-success",
  "comment": "Same diff as gate-path-failure but the gate is green — the arm must proceed (re-arm once green is the issue's own requirement).",
  "files": [
    {"filename": ".github/workflows/ci.yml", "previous_filename": null},
    {"filename": "tests/pi-intake-tick-collision-gate.test.sh", "previous_filename": null}
  ],
  "gate_globs": [
    ".github/workflows/**",
    ".github/scripts/**",
    ".github/CODEOWNERS",
    ".gitleaksignore",
    ".gitleaks.toml",
    ".semgrepignore",
    ".semgrep.yml",
    ".semgrep.yaml",
    ".fleet/**"
  ],
  "checks": [
    {"name": "P14 tests / PR checks", "bucket": "pass", "state": "SUCCESS"},
    {"name": "arm / arm", "bucket": "pass", "state": "SUCCESS"},
    {"name": "gate-integrity / gate-integrity", "bucket": "pass", "state": "SUCCESS"}
  ],
  "repo_has_gate": true,
  "expect": "arm"
}
