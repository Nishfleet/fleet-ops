The `file` dedupe corpus spanned all enrolled repos, so a same-titled open issue in another Nishfleet repo suppressed the filing entirely. Two fleet-ops Auto revert HALT runs (34653780914, 34654357872) delivered as comments on Nishfleet/0509#2923 (noise-class) and no fleet-ops halt ticket exists for either event.

Fix: `cmd_file` pins `collect_open(cross_repo=False)` — the dedupe corpus is the repo passed via `--repo` only. A cross-repo same-problem issue can never suppress or redirect a filing; `sweep` keeps its own clustering behaviour; `--no-cross-repo`/`--search-repo` remain accepted but no longer widen the file corpus.

Verification:
- `bash tests/fleet-issue-file-dedupe-repo-scope.test.sh` → ALL OK: fake gh with a same-titled open issue in 0509; `file --repo Nishfleet/fleet-ops` creates in fleet-ops (new number), never comments on 0509; same-repo duplicate still comments; ast guard pins cross_repo=False inside cmd_file
- `bash tests/issue-file.test.sh` → all OK (no regressions)
- `bash tests/fleet-issue-file-close-duplicates.test.sh` / `-idempotent` / `-regression-3161` and `tests/standards-drift-dedupe.test.sh` → pass, sweep clustering unchanged
- `sgscan` → no new security findings

run-proof: tests/fleet-issue-file-dedupe-repo-scope.test.sh (mechanism + guard, exit 0)

research: read lib/issue-file.py cmd_file/collect_open and all callers (.github/scripts/auto-revert.sh and bin/ pass only --repo, so no call-site change needed)
help-first: `bin/fleet-issue-file --help` green via tests/issue-file.test.sh before the change

mechanism: the new repo-scope test observes the boundary to close; the ast guard inside it is the cheap preventer pinning `cross_repo=False` in cmd_file (fleet-ops#366)

net-positive-because: the body of the fix is one pinned flag plus a dedicated boundary test and prevention guard (fleet-ops#5620 accept bullets require the new test file and the guard); swapping the corpus call for cross_repo=False is otherwise a two-line change

Gate note: this PR touches no gate-owned paths (.github/scripts/auto-revert.sh unchanged)

loose-ends: none

Closes #5620
