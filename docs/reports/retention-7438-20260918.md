# Log retention report — issue #7438, run 2026-09-18T10:20:53Z

Advisory only. Nothing deleted, no retention config changed, no service or timer added.

Five local artefact classes were enumerated and scored by the shared Jev helper
(`bin/jev-eval.mjs`, merged from #7466, invoked as `jev-eval` from PATH). Scoring ran
through the retention-report implementation from PR #7592, which is parked on the
salvage worktree at `/home/nish/workspaces/agent-worktrees/issue-7438` (`01fa75ef2`);
the census organ it extended was deleted from main on 2026-09-18 by the glue-sweep
(`f851c86cf`, "delete meta-metrics-scorers (9558 lines, Jev 0.73)", commit body names
asset-census), landed after the orchestrator's 2026-09-17 decision to extend it.

## Measured classes

| Class | Files | Decimal GB | Value, 0–4 | Keep probability | Proposed drop GB |
| --- | ---: | ---: | ---: | ---: | ---: |
| issue-inputs | 2368 | 0.049203439 | 3.19 | 0.96 | 0 |
| issue-outputs | 2352 | 0.004939179 | 2.99 | 0.92 | 0 |
| issue-errors | 2352 | 0.003067914 | 2.99 | 0.89 | 0 |
| watch-active | 1 | 0.006345113 | 3.68 | 0.96 | 0 |
| watch-rotated | 5 | 0.012575721 | 3.13 | 0.93 | 0 |

Proposed drop list: empty. Proposed savings: **0 GB**. Actual deleted bytes: **0**.
Keep probabilities are the helper's `action` margins from the run's JSONL receipts.

## Real records

- Command: `python3 lib/pi-packet/asset-census.py retention-report --output-json /tmp/retention-7438-20260918.json`, exit 0, `status: scored`.
- Measured paths include `/home/nish/.local/state/pi-issues/0509-1051.in` (inputs),
  `.out`/`.err` siblings (outputs/errors), `/home/nish/.local/state/pi-packet/watch.log`
  (active) and `watch.log.1`–`watch.log.5` (rotated by `config/logrotate.conf` via
  `systemd/pi-packet-logrotate.service`, timer live).
- Helper receipts: `~/.local/state/pi-packet/jev/artefact-retention.jsonl` rows 21–25,
  timestamps 2026-09-18T10:20:54Z–10:20:56Z, each with the class path/pattern, state
  hash, answers, probabilities, usage and duration. Non-synthetic.
- Route decision receipt: `~/.local/state/pi-packet/jev/retention-7438-route.jsonl`,
  ref `Nishfleet/fleet-ops#7438@2026-09-18T10:25Z`, route `paper-pr` p=0.69
  (restore-census-pr 0, blocked-orchestrator 0.31), termination-met p=0.90,
  state sha256 `4d77bad9a1c11ade1ac9340a26f160aee990fac9e8b39801246dce78649a2270`.
- Five SDK calls through the installed helper, cumulative shared budget after this run:
  $0.5232 of the $1 cap (`~/.local/state/pi-packet/jev/spend.json`, 3031 calls).
- Rollback switch verified live: `FLEET_RETENTION_ENABLED=0 … retention-report` prints
  the disabled record and exits 0 with no helper call.

## Unknowns stay unknown

Modification age is not creation age. Access times are not proof of a human read under
relatime/noatime, and scanners count as access. `last_read` stays null; per-file
consumer references stay unknown. Known class references: `systemd/pi-issue@.service`
consumes inputs and writes outputs/errors; `config/logrotate.conf` and
`systemd/pi-packet-logrotate.service` retain the active watch log plus five rotations.
Unknown does not mean unused.

R2 buckets, filesystem watchers and nested archives are unmeasured and named as such.
Byte totals are snapshots of changing files, not guaranteed reclaimable blocks.

## What this run does not do

No file is deleted, no retention config is changed, no service or timer is added.
The flip (applying any drop) stays locked behind Nish's or the weekly review's
one-time confirmation of a future list, per the issue. With a 0 GB proposed drop
there is nothing to flip; retention stays unchanged.

## Code home

The scoring implementation (retention-report subcommand + tests) is not on main: the
census organ that carried it was deleted by the orchestrator-driven glue-sweep
`f851c86cf` after PR #7592 was closed. Re-adding the organ would reverse a landed
sweep decision, so the report is delivered as paper and the implementation stays on
the salvage branch. Re-running it is reproducible from that worktree. If the fleet
ever wants the scorer on main, that is an orchestrator decision, not a worker one.
