test(rule-enforcement): add duplicate-source regression drill

`config/rule-enforcement.json` no longer lists the 2026-08-27 TOP GEAR or
escalation-matrix-FIXES source twice (the stub rows were removed and the
real rows were flipped to enforced in #583). Add a `validate-matrix` drill
that proves two rows sharing the same ledger source are rejected, so P14
cannot silently accept a stub + real pair again.

Prevention mechanism: the new `bad-duplicate.json` fixture in
`tests/rule-enforcement.test.sh` drives `lib/rule-enforcement.py
validate-matrix` and asserts rc=1 with a `duplicate source` error that
names the affected source.

Verification:
```
$ bash tests/rule-enforcement.test.sh
...
OK: validate-matrix: duplicate source is rejected
...
OK: rule-enforcement: matrix, join, stale queued, advisory, auto-file, observe-to-close, no-agent-names, vault-conflict, wipe-lessons, and cline-glm53 drills
```

Closes #561
