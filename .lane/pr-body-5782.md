# fix(#5782): drill throwaway units — remove the four 2026-09-12 leftovers, register-while-live, zero-leftover teardown proof

## Fix

The 2026-09-12 blind audit found four throwaway drill systemd user units alive after their stress finished: `btdrill-5471.path` (still active after #5471 closed), plus `resilience-drill-stub-{merge,postprobe,tlimit}-probe.path` (two loaded/waiting, one on disk). None was registered, so the machinery-authorization hunt burned 4 of its 32 hits on known leak noise, and the orphaned `.path` watchers burned wakeups on sentinels nobody would ever touch again.

1. **Live cleanup (the metric):** the four unit files are gone from `~/.config/systemd/user`, the manager reports `LoadState=not-found` for all four, and the production hunt no longer produces a single hit for them — its drill-unit hits dropped 32-noisy → 0. Stop + delete + daemon-reload semantics, matching the issue's rollback note (the audit report quotes the removed files; recreating + daemon-reload restores).
2. **Register-while-live (fleet-ops#366):** the seat_sentinel proof units (`resilience-drill-stub-seat-sentinel`, `-tiny`) — the standing drill's throwaway `.path`/`.service` pairs — are now registered in `config/machinery-allowlist.json` (class b, repo-sourced, endorsed #5106/#5096), so the machinery-authorization hunt stays silent for exactly the seconds they are live and stays LOUD if one ever outlives its drill.
3. **Zero-leftover teardown proof in the drill itself:** the `seat_sentinel` plane's teardown is now self-proving — after `cleanup_seat_sentinel_drill` (stop, reset-failed, rm, daemon-reload) it re-queries the manager (`show -p LoadState`) and the unit dir (`-e`), and bails LOUD if anything it created survived: nothing loaded, nothing on disk (the two 2026-09-12 leak symptoms, both path-proofed).
4. **Representative-drill regression:** `tests/fleet-resilience-drill.test.sh` gained a live representative run — `bin/fleet-resilience-drill --plane seat_sentinel` (new `--plane` single-plane mode) installs REAL throwaway `.path`/`.service` units into the manager, fires the sentinel storm, and the test independently asserts the teardown left zero leftovers: no unit file under the user unit dir, `LoadState=not-found` in the manager, `list-unit-files` clean of the stub namespace. Sessionless runners SKIP+LOUD (repo convention, the #5106 daily 05:47 timer owns the live proof there).
5. **Hunt stays the detector (accept 3):** `tests/machinery-authorization-gate.test.sh` pins the 2026-09-12 case — a real-file `.path` whose stem is NOT allowlisted is a hunt hit (evidence names the `.path` file itself), while the same shape with its stem registered register-while-live stays silent.

## Verification

- Termination: `systemctl --user list-unit-files --no-legend | grep -E 'btdrill-5471|resilience-drill-stub-(merge|post|tlimit)-?'` → no match; `systemctl --user show` for all four → `LoadState=not-found`; no seat-sentinel residue either after the test run.
- Production hunt on the deploy clone (`bash bin/fleet-machinery-authorization-gate hunt`) → 29 findings total, ZERO hits for the four drill units → the issue's metric is met live on this host (`XDG_RUNTIME_DIR=/run/user/1000`).
- `bash tests/machinery-authorization-gate.test.sh` → exit 0, incl. `OK: hunt classifies unregistered real-file .path units, registered ones stay silent (fleet-ops#5782)` (both sides of the fix pinned: unregistered real-file `.path` is a `kind=unit` hit with evidence naming the `.path` file; the registered seat_sentinel stub stays silent).
- `bash tests/fleet-resilience-drill.test.sh` → exit 0, including `OK: machinery-allowlist registers the seat_sentinel proof units (register-while-live)` and the live representative drill: `OK: live representative drill run: seat_sentinel pass, teardown left 0 leftover units (fleet-ops#5782)` (this run RE-PROVED it after rebase too). First version of the live section SKIPped on this very host — the harness redirects HOME to a scratch dir, so the plane's session guard never saw `.config/systemd/user` and the manager's unit search path never saw the written units; the fix restores the manager's HOME for the live block only (scratch stays for everything else). Act-on finding from this unit's own inner loop, fixed in commit 303ef0cf5, then re-run to green.
- `sgscan` → `No new security findings.`

run-proof: the live representative drill ran for real on this host against the user manager (XDG_RUNTIME_DIR=/run/user/1000, private socket present): the plane installed REAL `resilience-drill-stub-seat-sentinel{,.service,.path}` and `-tiny` throwaway units, daemon-reloaded, started the watcher, stormed it, and teardown left `grep resilience-drill-stub-seat-sentinel` over `systemctl --user list-unit-files` empty and no on-disk unit files — asserted twice (plane-internal seat_sentinel bail + test-external checks). Production hunt re-run post-fix (re-verified this salvage session, 2026-09-12T22:2xZ): 29 findings, zero for the four target names. Suites: `tests/fleet-resilience-drill.test.sh` exit 0, `tests/machinery-authorization-gate.test.sh` exit 0, both after the rebase on #6092's main.

net-positive-because: +177/−2 is executable proof, not paper: the plane's teardown now FAILS if its own run leaks a unit (the 2026-09-12 class), the test asserts leftovers twice from OUTSIDE the plane, two register-while-live rows quiet the hunt for the legitimate seconds-long drill stubs, and the `.path` classification case is pinned so the detector class that caught the leak can never silently stop working.

organ-heartbeat: bin/fleet-resilience-drill not-an-organ: the drill is a scheduled evidence plane, not a fleet organ — `systemd/fleet-resilience-drill.timer` is registered in config/fleet-organs.json, untouched by this diff; no unit/timer/path-unit file added or changed, no heartbeat metric or absent() rule touched.

loose-ends: none — the four live units are deleted and the termination condition passes; the drill-endpoint residue ("resilience-drill-stub-*-probe" sed-copies born from one-off #5471-side probe sessions) has no standing script on main to tear down, only the already-removed unit files; the crgate local run is the standing Nish-side CodeRabbit login gate (see Verification).

Test plan: `bash tests/machinery-authorization-gate.test.sh` and `bash tests/fleet-resilience-drill.test.sh` (repo convention for both touched paths; the live block exercises real systemd on session hosts, SKIPs LOUD on hosted CI where the #5106 daily 05:47 timer owns the live proof). No D1/KV/R2 surface touched; no vitest; shellcheck-class coverage among the suites above.

Closes #5782
