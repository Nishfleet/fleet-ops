fix(seat-health): floor overload bench at overload_bench_default_s after 3 consecutive Retry-After failures (fleet-ops#5430)

Observed live 2026-09-11T15:00-15:07Z: paretoinference/z-ai/glm-5.3-flash
returned 503 + Retry-After: 30 twenty-plus times; every failure
re-anchored usable_at = observed + 30s, so the heartbeat failed-requeue
tick re-picked the same hard-down seat every ~30s and blind-audit-cap-fix
died twice in 4 minutes (15:00:36Z and 15:04:07Z, exit 1, dead-man trip).

Fix (in the out-of-repo extension ~/.pi/agent/extensions/seat-health.ts —
the canonical copy per the fleet-ops#3263 decoupling; same
import-the-live-extension close shape as fleet-ops#1422 / #5096): in
computeUsableAt, when failure_mode=overload and
consecutive_failure_count >= 3, the bench is floored at
max(retry_after, overload_bench_default_s) so the wall outlives the
heartbeat requeue tick. Single-shot 503s (count < 3) still honor
Retry-After unchanged; a LONGER Retry-After is kept (floor, never a
cap); the quarantine escalation and comeback-release probe paths are
untouched. The issue's corpse-convergence question is answered and
pinned by the test: overload already converges to a seat_dead corpse at
the 25-failure threshold (OB5).

tests/seat-health-overload-bench.test.sh imports the live extension and
asserts the scenario from the issue (30, 30, floor >= 300 on the third
failure) plus the single-shot and longer-Retry-After invariants. The
suite is registered in tests/ci-standards-audit.test.sh so P14 runs it
without a workflow-file edit (the worker App cannot push
.github/workflows/**).

Closes #5430

Verification:
- bash tests/seat-health-overload-bench.test.sh → ALL OK, exit 0:
  OB1 30s, OB1b 30s, OB2 >=300 (got=300), OB3a/OB3b >=300, OB4 >=900
  (got=900), OB5 c24=false/c25=true seat_dead convergence
- bash tests/seat-health-quarantine.test.sh → ALL OK (unchanged)
- bash tests/seat-health-classifier.test.sh → ALL OK (unchanged)
- bash tests/seat-health-seat-dead.test.sh → ALL OK (unchanged)
- bash tests/seat-health-ledger-noop-write.test.sh → ALL OK (unchanged)
- bash tests/ci-standards-audit.test.sh → exit 0 (1134 OK lines,
  including the new OB1–OB5 hook)
- sgscan changes since origin/main → no new security findings
- bin/fleet-no-agent-names-check --commit-range origin/main..HEAD → OK

run-proof: unit pi-issue-fleet-ops-5430, one run, inner-loop 1 round to
green; no new units/timers/workflows introduced. Re-entrant recovery:
the head test commit is salvaged from closed PR #5432 (claim released by
heartbeat mid-run), rebased onto current origin/main (ccc2a0035); the
rebase conflict against the new #5096 audit hook was resolved by keeping
both hooks, and the full battery was re-run to green post-rebase.

Test plan: bash tests/seat-health-overload-bench.test.sh

net-positive-because: +172 lines is one 111-line test file asserting the
issue's proof scenario, its 10-line registration in the P14 audit suite,
and the PR body — the fix itself is a 6-line floor in the live extension
outside this diff.
