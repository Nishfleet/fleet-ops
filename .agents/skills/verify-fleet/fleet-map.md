# Fleet map


Do not print token files. a runner `.jit.env` is observed with `systemctl show` and `test -s`, never with `cat`.

| Unit | Trigger | Reads | Artifact | Observe | Healthy |
| --- | --- | --- | --- | --- | --- |
| <!-- fleet-map:agent.slice --> `agent.slice` | System slice. Parent of all 32 `actions.runner.Nishfleet.netcup-agent-1..32` runners (`Slice=agent.slice` in the runner drop-in). No unit of its own to start. | `systemd/system/agent.slice` | RAM cap `MemoryHigh=10G`/`MemoryMax=11G` and swap cap `MemorySwapMax=1G` (fleet-ops#8638). | `systemctl show agent.slice -p MemoryHigh -p MemoryMax -p MemorySwapMax -p MemorySwapPeak` | `MemoryHigh=10737418240`, `MemoryMax=11811160064`, `MemorySwapMax=1073741824`. `MemorySwapMax=infinity` means the 09-24/25 host-swap incident can return (8G of swap). |
| <!-- fleet-map:alert-repair@.service --> `alert-repair@.service` | A start of `alert-repair@<alertname>`. Oneshot, so a second start merges only while it is still running. | `prompts/alert-repair.md` on `worker-cheap`. Instance name is `FLEET_ALERTNAME`. | The repair the prompt applies (issue or pull request), in the journal of this invocation. | `journalctl --user _SYSTEMD_INVOCATION_ID=<id>` and `systemctl --user show alert-repair@<name> -p Result -p MemoryPeak -p CPUUsageNSec` | `Result=success`. A detached child is a failure: the unit would go dead while the repair still runs. |
| <!-- fleet-map:app-litellm-postgres.slice --> `app-litellm-postgres.slice` | Parent slice of `fleet-litellm-postgres.service`. | `systemd/app-litellm-postgres.slice` | The cgroup limit: `MemoryMax=1G`, `CPUQuota=100%`. | `systemctl --user show app-litellm-postgres.slice -p MemoryMax -p CPUQuotaPerSecUSec` | `MemoryMax=1073741824` and `CPUQuotaPerSecUSec=1s`. |
| <!-- fleet-map:app-litellm-redis.slice --> `app-litellm-redis.slice` | Parent slice of `fleet-litellm-redis.service`. | `systemd/app-litellm-redis.slice` | The cgroup limit: `MemoryMax=128M`, `CPUQuota=50%`. | `systemctl --user show app-litellm-redis.slice -p MemoryMax -p CPUQuotaPerSecUSec` | `MemoryMax=134217728` and `CPUQuotaPerSecUSec=500ms`. |
| <!-- fleet-map:app-litellm.slice --> `app-litellm.slice` | Parent slice of `fleet-litellm-proxy.service`. | `systemd/app-litellm.slice` | The cgroup limit: `MemoryMax=2G`, `CPUQuota=200%`. | `systemctl --user show app-litellm.slice -p MemoryMax -p CPUQuotaPerSecUSec` | `MemoryMax=2147483648` and `CPUQuotaPerSecUSec=2s`. |
| <!-- fleet-map:fleet-ci.slice --> `fleet-ci.slice` | User slice. The fleet-ci runners are system units and are not members of this slice. | `systemd/fleet-ci.slice` | Cohort cap `MemoryMax=9G`, `MemoryHigh=7G`, `CPUWeight=80`. | `systemctl --user show fleet-ci.slice -p MemoryMax -p MemoryHigh -p CPUWeight` | `MemoryMax=9663676416`, `MemoryHigh=7516192768`, `CPUWeight=80`. |
| <!-- fleet-map:fleet-container.slice --> `fleet-container.slice` | Parent slice of containerized workers. | `systemd/fleet-container.slice` | `MemoryMax=24G`. | `systemctl --user show fleet-container.slice -p MemoryMax` | When this file is the loaded slice: `MemoryMax=25769803776`. |
| <!-- fleet-map:fleet-litellm-postgres.service --> `fleet-litellm-postgres.service` | Enabled on `default.target`. `Restart=on-failure`. | Quadlet `containers/quadlet/fleet-litellm-postgres.container` (postgres:16), linked into `~/.config/containers/systemd/`. Data dir under the home share path in the file. | Postgres is up on its local socket. No network listener. | `systemctl --user show fleet-litellm-postgres.service -p ActiveState -p Result -p MemoryPeak` | `ActiveState=active`, `SubState=running`. oomd preference `avoid`. |
| <!-- fleet-map:fleet-litellm-proxy.service --> `fleet-litellm-proxy.service` | Enabled on `default.target`. `Restart=on-failure`. | Quadlet `containers/quadlet/fleet-litellm-proxy.container` (litellm image, pinned tag) mounting `config/litellm-proxy.yaml` from its live path. | `http://127.0.0.1:4000/health/readiness` returns success. Log level stays `INFO`. | `systemctl --user show fleet-litellm-proxy.service -p ActiveState -p Result -p MemoryPeak` and `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:4000/health/readiness` | `ActiveState=active`. Readiness HTTP 200. `TimeoutStopSec=120` from the Quadlet file. |
| <!-- fleet-map:fleet-litellm-redis.service --> `fleet-litellm-redis.service` | Enabled on `default.target`. | Quadlet `containers/quadlet/fleet-litellm-redis.container` (redis:7). | Redis answers on `127.0.0.1` only. | `systemctl --user show fleet-litellm-redis.service -p ActiveState -p Result` | `ActiveState=active`, `SubState=running`. oomd preference `avoid`. |
| <!-- fleet-map:fleet-sync.service --> `fleet-sync.service` | `.github/workflows/deploy-box.yml` on every push to main, and once at boot (`default.target`). | The deploy clone, `config/fleet_rules.yml`, `config/prometheus.yml`, `config/prometheus-am-executor.yml`, `config/user-tmpfiles.d/agent-worktrees.conf`. | A fast-forward of the deploy clone when `origin/main` moved, or the journal line that HEAD was unchanged. A dirty clone prints `DEPLOY-BLOCKED` and fails. | `journalctl --user -u fleet-sync.service -n 40 --no-pager` | `Result=success`. `DEPLOY-BLOCKED` or `LINK-GUARD` in that invocation is a failure. |
| <!-- fleet-map:hermes-gateway.service --> `hermes-gateway.service` | System unit. The repo ships only the clean-stop drop-in; install is a root `install` plus `daemon-reload`. | `systemd/system/hermes-gateway.service.d/60-clean-sigterm.conf` | `SuccessExitStatus=1 75` so an explicit SIGTERM stop stays inactive. Exit 75 remains the service-restart code. | `systemctl show hermes-gateway.service -p SuccessExitStatus -p ActiveState -p Result -p NRestarts` | While serving: `ActiveState=active` and `NRestarts=0`. After an explicit stop: `ActiveState=inactive` and `Result=success`. |
| <!-- fleet-map:oomd.conf --> `oomd.conf` | systemd-oomd global config. This is not a service unit. | `systemd/system/oomd.conf.d/99-fleet.conf` | `DefaultMemoryPressureDurationSec=60s` once the file is installed under `/etc`. | `systemctl show systemd-oomd.service -p ActiveState` and the installed drop-in | The repo file sets `DefaultMemoryPressureDurationSec=60s` and `systemd-oomd.service` is active. |
| <!-- fleet-map:tailscaled.service --> `tailscaled.service` | System unit. The repo only ships the drop-in; install is a root `install` plus `daemon-reload`. | `systemd/system/tailscaled.service.d/50-restart-always.conf` | `Restart=always` and `RestartSec=5s` on the system unit. | `systemctl show tailscaled.service -p Restart -p RestartUSec -p ActiveState` | `ActiveState=active` and `Restart=always`. SSH depends on this process. |
| <!-- fleet-map:tmux-spawn-.scope --> `tmux-spawn-.scope` | Scope drop-in for interactive tmux panes. | `systemd/tmux-spawn-.scope.d/50-interactive-cpu-priority.conf` | `CPUWeight=300` on those scopes. | `systemctl --user show <tmux-scope> -p CPUWeight` | The drop-in file sets `CPUWeight=300`. |
| <!-- fleet-map:user-1000.slice --> `user-1000.slice` | System slice for the whole user tree, including login sessions. | `systemd/system/user-1000.slice.d/50-ram-governor.conf` | `MemoryHigh=4G` and oomd kill at 80%. | `systemctl show user-1000.slice -p MemoryHigh -p ManagedOOMMemoryPressure -p ManagedOOMMemoryPressureLimit` | `MemoryHigh=4294967296`, `ManagedOOMMemoryPressure=kill`, `ManagedOOMMemoryPressureLimit=3435973836` (systemd's encoding of 80%). |
| <!-- fleet-map:user@1000.service --> `user@1000.service` | System unit for the user manager. | `systemd/system/user@1000.service.d/50-no-distro-oomd-kill.conf` | Distro oomd kill on this user manager is `auto`, not `kill`. | `systemctl show user@1000.service -p ManagedOOMMemoryPressure -p ActiveState` | `ActiveState=active` and `ManagedOOMMemoryPressure=auto`. |
