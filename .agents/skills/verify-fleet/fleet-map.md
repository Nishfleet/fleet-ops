# Fleet map


Do not print token files. a runner `.jit.env` is observed with `systemctl show` and `test -s`, never with `cat`.

| Unit | Trigger | Reads | Artifact | Observe | Healthy |
| --- | --- | --- | --- | --- | --- |
| <!-- fleet-map:alert-repair@.service --> `alert-repair@.service` | A start of `alert-repair@<alertname>`. Oneshot, so a second start merges only while it is still running. | `prompts/alert-repair.md` on `worker-cheap`. Instance name is `FLEET_ALERTNAME`. | The repair the prompt applies (issue or pull request), in the journal of this invocation. | `journalctl --user _SYSTEMD_INVOCATION_ID=<id>` and `systemctl --user show alert-repair@<name> -p Result -p MemoryPeak -p CPUUsageNSec` | `Result=success`. A detached child is a failure: the unit would go dead while the repair still runs. |
| <!-- fleet-map:app-litellm-postgres.slice --> `app-litellm-postgres.slice` | Parent slice of `fleet-litellm-postgres.service`. | `systemd/app-litellm-postgres.slice` | The cgroup limit: `MemoryMax=1G`, `CPUQuota=100%`. | `systemctl --user show app-litellm-postgres.slice -p MemoryMax -p CPUQuotaPerSecUSec` | `MemoryMax=1073741824` and `CPUQuotaPerSecUSec=1s`. |
| <!-- fleet-map:app-litellm-redis.slice --> `app-litellm-redis.slice` | Parent slice of `fleet-litellm-redis.service`. | `systemd/app-litellm-redis.slice` | The cgroup limit: `MemoryMax=128M`, `CPUQuota=50%`. | `systemctl --user show app-litellm-redis.slice -p MemoryMax -p CPUQuotaPerSecUSec` | `MemoryMax=134217728` and `CPUQuotaPerSecUSec=500ms`. |
| <!-- fleet-map:app-litellm.slice --> `app-litellm.slice` | Parent slice of `fleet-litellm-proxy.service`. | `systemd/app-litellm.slice` | The cgroup limit: `MemoryMax=2G`, `CPUQuota=200%`. | `systemctl --user show app-litellm.slice -p MemoryMax -p CPUQuotaPerSecUSec` | `MemoryMax=2147483648` and `CPUQuotaPerSecUSec=2s`. |
| <!-- fleet-map:app-pi-issue.slice --> `app-pi-issue.slice` | Parent slice of the pi issue workers. Filename on disk is `systemd/app-pi\x2dissue.slice`. | `systemd/app-pi\x2dissue.slice` | oomd pressure policy from the file: kill at 80%. Per-worker `MemoryMax` stays on the worker units. | `systemctl --user show app-pi-issue.slice -p ManagedOOMMemoryPressure -p ManagedOOMMemoryPressureLimit` | When this file is the loaded slice: `ManagedOOMMemoryPressure=kill` and `ManagedOOMMemoryPressureLimit=3435973836` (that integer is systemd's encoding of 80%). |
| <!-- fleet-map:fleet-ci.slice --> `fleet-ci.slice` | Parent slice of `gh-runner@`. | `systemd/fleet-ci.slice` | Cohort cap `MemoryMax=9G`, `MemoryHigh=7G`, `CPUWeight=80`. | `systemctl --user show fleet-ci.slice -p MemoryMax -p MemoryHigh -p CPUWeight` | `MemoryMax=9663676416`, `MemoryHigh=7516192768`, `CPUWeight=80`. |
| <!-- fleet-map:fleet-container.slice --> `fleet-container.slice` | Parent slice of containerized workers. | `systemd/fleet-container.slice` | `MemoryMax=24G`. | `systemctl --user show fleet-container.slice -p MemoryMax` | When this file is the loaded slice: `MemoryMax=25769803776`. |
| <!-- fleet-map:fleet-gardener.service --> `fleet-gardener.service` | `fleet-gardener.timer` Mondays 09:30 IST. | `prompts/gardener.md` | The gardener's journal for this invocation (memory note and unit census the prompt asks for). | `journalctl --user -u fleet-gardener.service -o short-iso` | `Result=success`. |
| <!-- fleet-map:fleet-gardener.timer --> `fleet-gardener.timer` | Installed on `timers.target`. | `systemd/fleet-gardener.timer` | The next elapse of `fleet-gardener.service`. | `systemctl --user list-timers fleet-gardener.timer` | `OnCalendar=Mon *-*-* 09:30:00` and the timer is active (waiting). |
| <!-- fleet-map:fleet-litellm-postgres.service --> `fleet-litellm-postgres.service` | Enabled on `default.target`. `Restart=on-failure`. | `systemd/fleet-litellm-postgres.service` and drop-in `systemd/fleet-litellm-postgres.service.d/20-oom-avoid.conf`. Data dir under the home share path in the unit. | Postgres is up on its local socket. No network listener. | `systemctl --user show fleet-litellm-postgres.service -p ActiveState -p Result -p MemoryPeak` | `ActiveState=active`, `SubState=running`. oomd preference on the drop-in is `avoid`. |
| <!-- fleet-map:fleet-litellm-proxy.service --> `fleet-litellm-proxy.service` | Enabled on `default.target`. `Restart=on-failure`. | `config/litellm-proxy.yaml` via the unit's config path, plus drop-ins `systemd/fleet-litellm-proxy.service.d/20-oom-avoid.conf`, `systemd/fleet-litellm-proxy.service.d/30-graceful-stop.conf`, `systemd/fleet-litellm-proxy.service.d/debug.conf`. | `http://127.0.0.1:4000/health/readiness` returns success. Log level stays `INFO`. | `systemctl --user show fleet-litellm-proxy.service -p ActiveState -p Result -p MemoryPeak` and `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:4000/health/readiness` | `ActiveState=active`. Readiness HTTP 200. `TimeoutStopSec=120` from the graceful-stop drop-in. |
| <!-- fleet-map:fleet-litellm-redis.service --> `fleet-litellm-redis.service` | Enabled on `default.target`. | `systemd/fleet-litellm-redis.service` and `systemd/fleet-litellm-redis.service.d/20-oom-avoid.conf`. | Redis answers on `127.0.0.1` only. | `systemctl --user show fleet-litellm-redis.service -p ActiveState -p Result` | `ActiveState=active`, `SubState=running`. oomd preference `avoid`. |
| <!-- fleet-map:fleet-sync.service --> `fleet-sync.service` | `fleet-sync.timer` every 2 minutes. | The deploy clone, `config/fleet_rules.yml`, `config/prometheus.yml`, `config/prometheus-am-executor.yml`, `config/user-tmpfiles.d/agent-worktrees.conf`. | A fast-forward of the deploy clone when `origin/main` moved, or the journal line that HEAD was unchanged. A dirty clone prints `DEPLOY-BLOCKED` and fails. | `journalctl --user -u fleet-sync.service -n 40 --no-pager` | `Result=success`. `DEPLOY-BLOCKED` or `LINK-GUARD` in that invocation is a failure. |
| <!-- fleet-map:fleet-sync.timer --> `fleet-sync.timer` | Installed on `timers.target`. | `systemd/fleet-sync.timer` | The next start of `fleet-sync.service`. | `systemctl --user list-timers fleet-sync.timer` | `OnCalendar=*:00/2` and the timer is active (waiting). |
| <!-- fleet-map:gh-runner@.service --> `gh-runner@.service` | Enabled instance `gh-runner@<n>` on `default.target`. One job, then the unit restarts with a new JIT registration. | The runner directory under `~/.local/share/actions-runner/<n>`. Do not print `.jit.env`. Slice `fleet-ci.slice`, `MemoryMax=3G`. | The runner process stays up between jobs (`./run.sh`). | `systemctl --user show gh-runner@<n> -p ActiveState -p Result -p MemoryPeak` | `ActiveState=active`. A dead instance is a failed runner. |
| <!-- fleet-map:oomd.conf --> `oomd.conf` | systemd-oomd global config. This is not a service unit. | `systemd/system/oomd.conf.d/99-fleet.conf` | `DefaultMemoryPressureDurationSec=60s` once the file is installed under `/etc`. | `systemctl show systemd-oomd.service -p ActiveState` and the installed drop-in | The repo file sets `DefaultMemoryPressureDurationSec=60s` and `systemd-oomd.service` is active. |
| <!-- fleet-map:pi-packet@.service --> `pi-packet@.service` | A start of `pi-packet@<id>` with `PI_PROVIDER` and `PI_MODEL` set. | Stdin file `~/.local/state/pi-packets/<id>.in`. | `~/.local/state/pi-packets/<id>.out` and `<id>.err`. | `systemctl --user show pi-packet@<id> -p Result -p ExecMainStatus -p MemoryPeak -p CPUUsageNSec` | `Result=success`. `TimeoutStartSec=45min`. Restart on failure, burst 3 per hour. |
| <!-- fleet-map:pi-scout-repair@.service --> `pi-scout-repair@.service` | A start of `pi-scout-repair@<repo>`. | `prompts/scout-repair.md` on `worker-cheap`. | The repair note in this invocation's journal. | `journalctl --user -u pi-scout-repair@<repo>` | `Result=success`. |
| <!-- fleet-map:pi-scout@.service --> `pi-scout@.service` | `pi-scout@.timer` every 4 hours, instance `<repo>`. | `prompts/scout.md` and `AGENTS.md`, deploy clone read-only. Mints the worker app token. Do not print it. Drop-in `systemd/pi-scout@.service.d/10-keystone-hc.conf`. | A journal line matching `supply: ready_count=<n>` for this invocation. | `journalctl --user _SYSTEMD_INVOCATION_ID=<id> --no-pager` and `systemctl --user show pi-scout@<repo> -p Result -p MemoryPeak -p CPUUsageNSec` | `Result=success` and the supply line is present. No supply line is `Result=exit-code` and the journal contains `scout-abort-gate:`. |
| <!-- fleet-map:pi-scout@.timer --> `pi-scout@.timer` | Installed on `timers.target`. | `systemd/pi-scout@.timer` | The next start of `pi-scout@<repo>.service`. | `systemctl --user list-timers 'pi-scout@*.timer'` | `OnCalendar=*-*-* 00/4:00:00` and the timer is active (waiting) for each enrolled repo. |
| <!-- fleet-map:tailscaled.service --> `tailscaled.service` | System unit. The repo only ships the drop-in; install is a root `install` plus `daemon-reload`. | `systemd/system/tailscaled.service.d/50-restart-always.conf` | `Restart=always` and `RestartSec=5s` on the system unit. | `systemctl show tailscaled.service -p Restart -p RestartUSec -p ActiveState` | `ActiveState=active` and `Restart=always`. SSH depends on this process. |
| <!-- fleet-map:tmux-spawn-.scope --> `tmux-spawn-.scope` | Scope drop-in for interactive tmux panes. | `systemd/tmux-spawn-.scope.d/50-interactive-cpu-priority.conf` | `CPUWeight=300` on those scopes. | `systemctl --user show <tmux-scope> -p CPUWeight` | The drop-in file sets `CPUWeight=300`. |
| <!-- fleet-map:user-1000.slice --> `user-1000.slice` | System slice for the whole user tree, including login sessions. | `systemd/system/user-1000.slice.d/50-ram-governor.conf` | `MemoryHigh=12G` and oomd kill at 80%. | `systemctl show user-1000.slice -p MemoryHigh -p ManagedOOMMemoryPressure -p ManagedOOMMemoryPressureLimit` | `MemoryHigh=12884901888`, `ManagedOOMMemoryPressure=kill`, `ManagedOOMMemoryPressureLimit=3435973836` (systemd's encoding of 80%). |
| <!-- fleet-map:user@1000.service --> `user@1000.service` | System unit for the user manager. | `systemd/system/user@1000.service.d/50-no-distro-oomd-kill.conf` | Distro oomd kill on this user manager is `auto`, not `kill`. | `systemctl show user@1000.service -p ManagedOOMMemoryPressure -p ActiveState` | `ActiveState=active` and `ManagedOOMMemoryPressure=auto`. |
