## What was wrong

Heartbeat 2026-08-30T17:30Z: opencode/muse-spark-1.2-contributor-free was
seat_dead=true, health_class=corpse, 150 consecutive 500s — yet still read
as "still on a 24h retry cycle". The corpse threshold (>=25 consecutive
failures -> seat_dead=true + terminal "corpse" class, fleet-ops#2145/#2327)
already existed, but the corpse ledger entry KEPT `usable_at` at the 24h
quarantine cap written by computeUsableAt, so every reader (heartbeat
included) saw a corpse with a comeback clock — a corpse scheduled for
re-probe forever while consuming probe budget.

The mechanical fix the issue asked for (a failure-count threshold that
promotes transient_http to permanent retirement) exists; the **permanent**
half was missing: a corpse still carried a retry clock.

## Fix

Out-of-repo live extension at ~/.pi/agent/extensions/seat-health.ts
(allowlisted, closure-tested from this repo like #1466/#1422/#2145):

- `writeSeatLedgerEntry`: the moment `shouldMarkSeatDead` fires the corpse
  now gets `usable_at = null` — a corpse is retired, it carries NO comeback
  clock. The 1422 quarantine wall is for WALLED seats that may recover,
  never a retry schedule for a corpse. Recovery is manual-only since the
  seat-walled-probe weekly probe organ was deleted (fleet-ops#2394).
- A further failure write on the corpse keeps the clock null (no reader can
  resurrect a retry cycle).
- Routing is unchanged: seat_usable / pick_seat exclude seat_dead=true
  before any usable_at check, so the cleared clock changes nothing for the
  router.

In-repo:

- `tests/seat-health-seat-dead.test.sh`: D10 now asserts the corpse
  record's usable_at is null (was: 24h cap) and adds D14 — a 26th failure
  keeps seat_dead=true / class corpse / usable_at null; D11 (healthy write
  recovers) and D12 (below-threshold quarantine wall unchanged at c=24,
  57600s step) still hold.
- `config/seat-caps.json` `_comment_seat_dead` documents the #2415
  contract.
- Live corpse ledgers backfilled to `usable_at: null` with `.bak` copies
  (opencode__muse-spark-1.2-contributor-free, devin__glm-5-2), so the
  heartbeat reads them as retired immediately, not after the next deploy.

## Verification (run-loop, all live)

1. `bash tests/seat-health-seat-dead.test.sh` — GREEN (all D1-D14).
   Previously-failing-at-fix assertion:
   `OK: D10: c25-usable (corpse carries NO retry clock — fleet-ops#2415 permanent retirement) (null)`
   (before the fix this asserted the 24h cap "86400").
2. `bash tests/seat-health-quarantine.test.sh` — GREEN (I1-I7; the 1422
   exponential wall for walled seats is unchanged).
3. `bash tests/seat-health-classifier.test.sh` — GREEN (I-invariants).
4. `bash tests/seat-lib.test.sh` + `bash tests/seat-lib-org-reserve.test.sh`
   — GREEN (routing read side unaffected).
5. End-to-end proof of the exact live scenario (scratch ledger, 150 writes
   like muse-spark):
   `RESULT_JSON:{"count":150,"seat_dead":true,"health_class":"corpse","usable_at":null,"failure_mode":"transient_http"}`
   then `seat_usable opencode muse-spark-1.2-contributor-free` ->
   `RESULT: UNUSABLE (corpse correctly excluded by seat_dead=true)`.
6. Live ledgers after backfill:
   `opencode__muse-spark-1.2-contributor-free.json` and
   `devin__glm-5-2.json` both `usable_at: null`; seat_usable says UNUSABLE
   for both.
7. `sgscan` on the diff: `No new security findings.` (rc=0)

run-proof: closure tests exit 0 (transcripts above); live ledger
`/home/nish/workspaces/agent-state/lanes/seats/opencode__muse-spark-1.2-contributor-free.json`
now carries `"usable_at": null` (observed live 2026-08-30T18:2xZ).

Related findings filed separately (out of scope): the unjustified-wait
audit flags corpse-class seats as "dead marker without credentials_bad
(clock inconsistent)" (fleet-ops#2327 class not exempted), and transient
corpses are not surfaced by FleetDeadCredentialSeats (it matches
credentials_bad class only).

Closes #2415