fix(failed-command): lock the live #793 fresh-debug-script exit-1 walked-past shape

The session-close lint (fleet-ops#535) auto-filed #793 from the
2026-08-27T01-15-01 session (slug 01a040c8-...): a worker on
issue-fleet-ops-622 / -690 used the write tool to create
/tmp/fsr-debug.sh (a Python-patch + bash-test harness), then ran
`bash /tmp/fsr-debug.sh` against it. The script's `set -euo pipefail`
killed it on the first error before any echo, the harness reported
`(no output)  Command exited with code 1`, and the agent continued
with `bash -x /tmp/fsr-debug.sh 2>&1 | tail -20` (a toolCall, no
user-facing text) without naming the failure.

The detector already correctly flags the live #793 class: the command
text does not match `BENIGN_STAGE_RE` (no grep/rg/diff), `LS_BENIGN_RE`
(no ls/which probe), `HARNESS_BLOCK_RE` does not match
(`(no output)  Command exited with code 1` is not a SPAWN_BLOCKED),
and no later assistant text names the failure. This commit pins that
shape so a future refactor cannot regress it. No
`lib/failed-command-flagged.py` change — a suppression would silence
a real signal. The auto-filed issue closes via observe-to-close
(fleet-ops#758) when the session mtime ages out of the 24h window
after 2026-08-27T01:15:01Z.

Changes:
- tests/fleet-failed-command-fresh-debug-script.test.sh: four
  scenarios lock the fresh-debug-script class. 1 replays the exact
  #793 `bash /tmp/<script>` snippet and proves the detector flags
  it (count=1, snippet carries the `(no output)` + exit-1 signal).
  2 replays the same shape plus a later "bash /tmp/... call failed
  with exit 1" user-facing flag and proves the detector does NOT
  flag it (count=0). 3 confirms that `bash /tmp/<script> || true`
  is still a finding (the harness still reports isError=true on
  the original invocation). 4 is the cross-check that the
  `which` no-match probe stays benign — pinning the contrast
  between the fresh-script shape and a probe.
- tests/seat-lib.test.sh: nest the new file in the CI host (worker
  token cannot add a P14 line in ci.yml; seat-lib is the listed
  runner).
- prompts/worker.md: extend the failing-command hard rule to spell
  out the fresh-debug-script exit-1 class with #793 as the live
  example.

Verification:
- bash tests/fleet-failed-command-fresh-debug-script.test.sh -> exit 0
  (4 OKs: live #793 fresh debug script exit 1 walked past is flagged,
  fresh debug script exit 1 plus later user-facing flag is clean,
  fresh debug script exit 1 with `|| true` walked past is flagged,
  which no-match probe is benign — contrast with fresh-debug-script shape).
- bash tests/seat-lib.test.sh -> exit 0 (CI host exists, the new
  fleet-failed-command-fresh-debug-script.test.sh nested in the
  seat-lib test runner, all other nested tests pass).
- bash tests/fleet-failed-command-flagged.test.sh -> exit 0 (the
  original detector test still passes — no regression).
- /home/nish/.local/bin/sgscan origin/main...HEAD -> "No new
  security findings."

run-proof: bash tests/fleet-failed-command-fresh-debug-script.test.sh
4 OKs / 4 scenarios, exit 0. bash tests/seat-lib.test.sh exit 0.

Closes #793
