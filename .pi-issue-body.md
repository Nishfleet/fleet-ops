fix(failed-command): lock the live #727 `npm run canary:bet2` exit-1 walked-past shape

The session-close lint (fleet-ops#535) auto-filed #727 from the
2026-08-27T00-01:33 session (slug 01a04085-9c3b-7194-a332-20e86c213e81):
a `npm run canary:bet2` call against the BET 2 verification canary
returned 1 (gate FAIL: `zero_dead_ends` observed 2 > threshold 0), the
toolResult was isError=true, and the assistant continued diagnosing
with curl probes without naming the canary failure in the user-facing
text.

The original session also ran a `for domain in ...; do curl ... done`
probe loop that exited 1 because the last `curl` returned 0 bytes,
but that loop is correctly classified as a grep no-match by
`is_benign_no_match` and is out of scope for this lock. The canary
class is a different shape: a real script that exits 1 to signal a
failed gate, not a search-style no-match.

The detector already correctly flags the live #727 class: the command
text does not match `BENIGN_STAGE_RE` (no grep/rg/diff), `isError` is
true, and no later assistant text names the failure. This PR pins
that shape so a future refactor cannot regress it. No
`lib/failed-command-flagged.py` change — a suppression would silence
a real signal. The auto-filed issue closes via observe-to-close
(fleet-ops#758) when the session mtime ages out of the 24h window
after 2026-08-27T00:01:33Z.

Changes:
- tests/fleet-failed-command-canary-script-exit-1.test.sh: four
  scenarios lock the canary exit-1 class. 1 replays the exact #727
  `npm run canary:bet2` snippet and proves the detector flags it
  (count=1). 2 replays the same shape plus a later "the canary
  returned 1, gate failed" flag and proves the detector does NOT
  flag it (count=0). 3 is the cross-check on a direct `node
  scripts/*-verification.mjs` exit 1 walked past. 4 confirms that
  piping a canary's output through `| tail -1` is still a finding
  (the failed stage is the script, not the pipe tail).
- tests/seat-lib.test.sh: nest the new file in the CI host (worker
  token cannot add a P14 line in ci.yml; seat-lib is the listed
  runner).
- prompts/worker.md: extend the failing-command hard rule to spell
  out the canary-script-exit-1 class with #727 as the live example.

Verification:
- bash tests/fleet-failed-command-canary-script-exit-1.test.sh -> exit 0
  (4 OKs: live #727 canary exit 1 walked past is flagged, canary
  exit 1 plus later user-facing flag is clean, node verification
  script exit 1 walked past is flagged, canary piped through tail
  exit 1 walked past is flagged).
- bash tests/seat-lib.test.sh -> 72 passed, 0 failed (the seat-lib
  CI host passes 14 of its own; the 4 nested fleet-failed-command
  test files all OK; the 6 nested exec-review / heartbeat / siterep
  / gate-integrity / decisions-ledger / findings-queued tests all
  OK).
- bin/fleet-no-agent-names-check --pr-body .pi-issue-body.md
  --commit-range origin/main..HEAD -> OK: no agent attribution
  detected.
- bin/fleet-wipe-lessons-check scan --root . -> clean under .

Closes #727
