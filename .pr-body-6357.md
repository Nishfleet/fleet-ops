**The chain-stalling 3×-empty vote is now diagnosable and storm-tolerant.**

`pi-audit-run` exited 1 on a malformed auditor output and its EXIT trap deleted the combined output — and the unit's `PrivateTmp` dies with the unit anyway, so dropping the trap would not have helped. A 3×-empty vote (chain `ae0681d0c947`, issue #6236: three consecutive empty verdicts, `StartLimitBurst=2` exhausted 14:25:41) was undiagnosable post-hoc and left the escalation chain stalled on a failed unit.

**The fix (both inside the existing runner, no new organ):**

1. **Evidence kept.** Every malformed-output strike (empty verdict or empty reason) copies the combined output to `<candidate dir>/<role>.strike-logs/<UTCts>-strike<N>.log`, bounded to the newest 5, and the journal line names the path — the third strike's WHAT is readable after the fact.
2. **Third strike SKIPs.** Consecutive strikes are counted per repo/candidate/role in `<role>.strikes`. Strikes 1–2 keep the unit's designed exit-1 retry-once contract. The 3rd consecutive strike writes the storm-tolerant SKIP vote (same semantics as the provider-wall exit≠0 path, #146) and exits 0: the #616 heartbeat recover path (reset-failed + start) — which is what produced the manual run 3 in the incident — now resolves the unit green, and the #3962 SKIP-recast ladder owns recovery (stale SKIP → recast → skip-exhausted → tally decides 2-of-2) instead of the chain sitting stalled. A real vote resets the streak.

The state files avoid every existing glob in the vote tree (`*.vote`, `<role>.vote.skip-*`, `<role>.skip-exhausted`, `.pending`).

Drive-by (found by the new test, one word): `seat_health_ok_for_call` declared `f` uninitialized — under `set -u` the no-seat-lib path died with `f: unbound variable` before the model call. Masked in production only because `litellm-seat.sh` always exists on a healthy install.

Verification:

- `bash tests/pi-audit-run.test.sh` — all 20 scenarios OK, incl. new 10a/10b/10c (strikes 1–2 exit 1 + raw output kept + counter; strike 3 SKIP + exit 0 + counter reset + evidence kept) and 11 (a real vote resets the streak)
- `tests/pi-audit-run-question.test.sh`, `tests/pi-audit-run-strip-preamble.test.sh`, `tests/pi-audit-run-product-repo-reality.test.sh` — PASS, no regression to verdict extraction
- `shellcheck -S warning bin/pi-audit-run` — rc=0

run-proof: tests/pi-audit-run.test.sh scenario10a/10b/10c + scenario11 OK lines from the committed suite (strike 1–2 exit-1 retry contract preserved, strike 3 SKIP + exit 0, streak reset on a real vote); shellcheck rc=0

Closes #6357

loose-ends: none — strike evidence, 3-strike SKIP, and reset-on-success are covered by the committed scenarios; the #3962 recast ladder is the existing observe-to-close for the SKIP itself.
