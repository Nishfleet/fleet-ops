## Summary

`count()` of an empty PromQL vector is absent, not zero. The duty-officer recordings `fleet_failed_units` and `fleet_seat_unhealthy_count` therefore went silently null whenever the fleet was healthy, so the hourly judge could not cite them.

Each of the three judge-cited names now uses `or vector(0)`. `fleet_workers_active` is `sum(...)` first so a live labeled series does not dual-emit with an unlabeled 0.

No new exporter. Source series already exist (`node_systemd_unit_state`, `fleet_pi_seat_healthy`, `fleet_pi_workers_active`).

## Test plan

- [x] `bash tests/fleet-duty-officer-recording.test.sh` — citations resolve; promtool: zero failed units -> 0; one failed unit -> 1
- [x] `bash tests/p14-test-listing-gate.test.sh` — new test hosted and pinned
- [x] `promtool check rules config/fleet_rules.yml` — SUCCESS: 101 rules found
- [x] Live PromQL of the new exprs returns a value, not null (below)

## Verification

```
$ bash tests/fleet-duty-officer-recording.test.sh
cited=fleet_failed_units,fleet_seat_unhealthy_count,fleet_workers_active
OK: citations resolve; or vector(0) on all three
OK: promtool check rules: fleet_rules.yml valid
OK: promtool: zero failed units -> 0
OK: promtool: one failed unit -> 1
exit 0
```

```
$ promtool check rules config/fleet_rules.yml
Checking config/fleet_rules.yml
  SUCCESS: 101 rules found
```

Live query of the post-fix exprs (recording names fill after deploy reloads Prometheus). `systemctl --user --state=failed` was empty (0 lines).

```
$ curl -sG http://127.0.0.1:9090/api/v1/query --data-urlencode 'query=count(node_systemd_unit_state{state="failed"} == 1) or vector(0)'
{"status":"success","n":1,"value":[1788808052.705,"0"]}

$ curl -sG http://127.0.0.1:9090/api/v1/query --data-urlencode 'query=count(fleet_pi_seat_healthy == 0) or vector(0)'
{"status":"success","n":1,"value":[1788808052.736,"0"]}

$ curl -sG http://127.0.0.1:9090/api/v1/query --data-urlencode 'query=sum(fleet_pi_workers_active{kind="sum"}) or vector(0)'
{"status":"success","n":1,"value":[1788808073.875,"8"]}
```

sgscan vs origin/main: No new security findings, rc=0.

The crgate call failed with CodeRabbit is not signed in on this machine (rc 3). CI CodeRabbit covers it.

## Gates

run-proof: bash tests/fleet-duty-officer-recording.test.sh exit 0 (promtool zero->0 and one->1); live PromQL of the three exprs returned "0","0","8" not null; promtool check rules SUCCESS 101 rules.

net-positive-because: +213 lines are the detector (tests/fleet-duty-officer-recording.test.sh) plus host/pin lines so a renamed judge metric fails CI instead of going silently null. The rules change itself is +8/-3.

organ-heartbeat: config/fleet_rules.yml not-an-organ: recording expr fix only; no new timer, exporter, or canary.

loose-ends: none — recorded series populate after fleet-deploy-check HUPs Prometheus.

Closes #4394
