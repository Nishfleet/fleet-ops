Fixes #6610

## What

The fleet's two rulebook layers gave opposite answers to the same question — "present a plan, then what?"

- The stop-for-approval layer (`~/.claude/CLAUDE.md` pre-implementation block + vault contract §2/§5) said: stop and wait for Nish's approval on every non-trivial task.
- The act layer (`/home/nish/AGENTS.md`, `~/.pi/agent/AGENTS.md`, vault `global-standing-rules.md` → "Engineer reversibility, don't gate" (Nish, 2026-08-24) and "Only the un-fixable reaches Nish" (Nish, 2026-08-18)) said: act, don't ask; only reserved classes and irreversible work come back as a question.

The contract's only reconciliation clause ("Higher-priority live system and repository instructions still win") named no precedence order, so Claude-loaded and Pi-loaded surfaces disagreed on the fleet's most common decision.

## Consolidation (as directed by the issue)

The stop-gate is now scoped to the canonical reserved classes and irreversible work, identical on every surface:

- **Vault contract §2** (`pre-implementation-contract.md`): "Then stop and wait for Nish's approval" now applies only to canonical reserved-class or non-reversible work; every other non-trivial task begins once Goal/Blocking questions/Assumptions/Plan are on the record. Header renamed "Produce This and Stop" → "Produce This Plan First".
- **Vault contract §5**: the orchestrator no longer claims it "obtained Nish's approval before dispatch" — it satisfies §2 before dispatch, waiting only where the work is reserved-class or irreversible.
- **CLAUDE.md render** (fleet-ops `lib/standing-rules/canonical.md`, SECTION `nish-preimplementation-contract`): same carve-out, cites "Engineer reversibility, don't gate" (Nish, 2026-08-24) and the vault "Canonical reserved-classes list" (Nish, 2026-09-11). Also lands on `~/.codex/AGENTS.md`, which shares the section.
- **Both Pi agent surfaces** (`lib/pi-agents-md/canonical.md` → `/home/nish/AGENTS.md` + `~/.pi/agent/AGENTS.md`): the "Act, don't ask" bullet now states the identical scope — the contract's stop-gate carries it too.

The contract's Enforcement clause ("Record any deliberate future change as a durable decision requested by Nish; agents cannot change it themselves") is satisfied: this amendment is issue #6610 on Nish's account, and it reconciles the contract with two recorded Nish rulings (2026-08-24 reversibility, 2026-09-11 reserved-classes consolidation) rather than inventing a new policy.

## Live surfaces updated and proven (render is the sanctioned path — headers of all four files say "edit the canonical and re-render")

All four rendered surfaces were re-rendered from the PR's canonical sources using the PR's own render scripts, then diffed against pre-change backups (`*.pre-issue-6610-20260914T004147Z`, actions.log line written):

- `~/.claude/CLAUDE.md` — one line changed, inside the `nish-preimplementation-contract` GENERATED region only
- `~/.codex/AGENTS.md` — one line changed, same region
- `/home/nish/AGENTS.md` — "Act, don't ask" bullet extended only
- `~/.pi/agent/AGENTS.md` — byte-identical to `/home/nish/AGENTS.md`

Verification: both render scripts' byte-level drift checks pass against the PR canonical (`render-standing-rules --check` → "OK (checked): 2 target(s), 6 section(s)"; `render-pi-agents-md --check` → "OK (checked): 2 target(s) match canonical").

run-proof: render-standing-rules --check OK (2 targets, 6 sections); render-pi-agents-md --check OK (2 targets); live-file diffs vs backups show only the intended regions changed; backups at *.pre-issue-6610-20260914T004147Z + actions.log entry

loose-ends: vault-git-remote-404

## Loose ends

`loose-ends: vault-git-remote-404` — during this fix, `git fetch` in the vault reported `repository 'https://github.com/Nishfleet/nish-vault.git/' not found`. Not touched here (out of scope); the vault's live sync is Syncthing, so this is likely a stale/renamed remote, but it should be confirmed or cleaned up. Filed separately.

## Test plan

- `python3 bin/render-standing-rules.py --canonical lib/standing-rules/canonical.md --check` → OK
- `python3 bin/render-pi-agents-md.py --check` → OK
- Docs-only change to two canonical .md files; no code paths touched.
