## Problem

The canonical `idle-fleet-alarm` block (fleet-ops `lib/standing-rules/canonical.md`, SECTION: idle-fleet-alarm — the single-edit-point fleet live-state check per fleet-ops#5748) claimed: "`agent-state/lanes/` now holds only `pi-seat-health.json`". False on live inspection: the directory holds 122 entries — `seats/`, `reports/`, `outbound-gate/`, `nish-boundary-notify.seen`, `fleet-question-stale.seen`, `fleet-question-unfiled.seen`, `money-boundary-pages.log`, `fleet-escalation-drain.over-bound`, `pi-seat-health.json`, and ~110 timestamped `seats-corpse-retired-*`/`seats-quarantine-*`/`seats-bench-poisoned-*` dirs (verified 2026-09-14: `ls /home/nish/workspaces/agent-state/lanes/ | wc -l`). An agent performing the canonical step-4 check sees a directory that contradicts the rulebook — the same distrust-training failure mode as the 2026-08-23 stale "fleet is PAUSED" text.

## Fix (smallest durable)

- `lib/standing-rules/canonical.md` (SECTION: idle-fleet-alarm only): the block now states the canonical live file — `agent-state/lanes/pi-seat-health.json` — without the "only" claim, and names the directory's operational-artefact shape generically (seats/, reports/, outbound-gate/, `.seen` markers, logs, timestamped seats-quarantine/corpse dirs) as "none of which this check reads" (fleet-ops#6613). The 5-step check, its order, and the FLEET-PAUSED-first sentinel are untouched.
- Detector/gate (fleet-ops#366 mechanical-fix): new assertion 13 in `tests/standing-rules-drift.test.sh` fails the suite if the idle-fleet-alarm section ever claims `lanes/` "holds only" one file again — same grep-gate pattern as the fleet-ops#5537/#5717 assertions.
- Live render: both generated surfaces (`~/.claude/CLAUDE.md`, `~/.codex/AGENTS.md`) rendered from the fixed canonical via the existing generator (`bin/render-standing-rules.py --render --canonical lib/standing-rules/canonical.md` → `OK (rendered): 2 target(s), 6 section(s)`), so the live surfaces already match the merged canonical the moment this lands; the `standing-rules-render.path` unit keeps them in sync thereafter.

## Verification

- `bash tests/standing-rules-drift.test.sh` → `ALL OK: 13/13 assertions passed (drift, render, templating, markers, orphans, pi-count pin, governed-run pin, retired-identity, fleet-paused sentinel, lanes-only claim)` (exit 0)
- Detector drill (gate proven live, both directions): same suite run against the pre-fix canonical (fixture of `HEAD~` text) → exit 1 with `FAIL: idle-fleet-alarm claims lanes/ 'holds only' one file (fleet-ops#6613) - state the canonical live file (pi-seat-health.json) without the 'only' claim; the live directory holds operational artefacts alongside it`
- `bin/sgscan` → `No new security findings.`
- `bin/render-standing-rules.py --check --canonical lib/standing-rules/canonical.md` → `OK (checked): 2 target(s), 6 section(s)`
- Stale-claim probe on the live surfaces: `grep -n "holds only" ~/.claude/CLAUDE.md ~/.codex/AGENTS.md` → no match (exit 1); new text present in both (1 hit each of the new wording)
- `bash tests/rulebook-host-drift.test.sh` → exit 0; `bash tests/one-fleet-rule-pointer.test.sh` → exit 0; `bash tests/p14-test-listing-gate.test.sh` → exit 0 (one transient glob-host flake on `pi-issue-run-senior-review-routing.test.sh` in a first run; that test passes standalone in this tree and on the untouched main checkout, and the gate re-ran green)
- Mid-run main movement, handled: PR #6621 (fleet-ops#6610) merged while this branch was in flight; the branch was rebased onto the new `origin/main` (final diff is exactly the two files above) and the live surfaces were re-rendered from the rebased canonical, so they now carry both the #6613 fix and #6610's reworded pre-implementation text. The `tests/reserved-classes-precedence.test.sh` live-target drift that existed at session start (pi-agents-md canonical lagged the live surfaces) resolved with merged #6621: the full suite is green post-rebase, including `OK: standing-rules render check green against the repo canonical` and `OK: pi-agents-md render check green`.

## run-proof:

- units/timers/workflows touched: none. The render pipeline is pre-existing (`systemd/standing-rules-render.path` + `standing-rules-render.service`, active/waiting on this host, verified this run).
- Real runs performed and green: drift suite 13/13 (exit 0), detector drill fail-proof (exit 1 on the old text, named reason), `render-standing-rules.py --render` + `--check` on the 2 live targets, `sgscan` (no new findings), stale-claim grep probes on both live surfaces.

## research:

Prior art existed and was reused end to end: the fleet-ops#5748 canonical+render+gate convention for exactly this block (edit point, byte-exact drift checks, FLEET-PAUSED-first assertion, live render before merge, in-tree `.pr-body.md` precedent) and the standing-rules grep-gate pattern from assertions 10–12 (fleet-ops#5537/#5747/#5717). No new machinery, no new `bin/` files, no new units.

## help-first:

`bin/render-standing-rules.py --help`, `bin/prove-one-run-check --help`, `bin/fleet-exec-review-canary --help`, `bin/fleet-token-efficiency-check --help`, `bin/research-before-build-check --help`, and `bin/fleet-no-agent-names-check --commit-range origin/main..HEAD` were exercised this run before use.

## Test plan

- `bash tests/standing-rules-drift.test.sh`
- `bash tests/rulebook-host-drift.test.sh`
- `bash tests/one-fleet-rule-pointer.test.sh`
- `bin/sgscan`
- `bin/render-standing-rules.py --check --canonical lib/standing-rules/canonical.md`

## Reviewer adjudication

Fleet-ops PRs are exempt from the product-reviewer round (config/intake-repos.json).

## Scope

Closes #6613

## loose-ends:

`loose-ends: none-open` — the session-start pi-agents-md canonical lag resolved with merged #6621 plus the re-render described above; nothing left open by this PR.
